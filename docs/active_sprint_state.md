## Web AI State
- **Overarching Goal:** Tauri + Vite Architecture Migration (Capture Studio Parity & Pipeline Stabilization).
- **Last Edited:** `desktop-studio/src/capture_pane.js`, `desktop-studio/src/detail_pane.js`, `desktop-studio/src/main.js`, `desktop-studio/src/ipc_bridge.js`, `desktop-studio/src/styles.css`, `desktop-studio/index.html`, `desktop-studio/src-tauri/src/capture_manager.rs`, `desktop-studio/src-tauri/src/lib.rs`, `analysis/src/localization.rs`.
- **Unresolved Errors/Bugs:** None blocking — see Immediate Sprint Focus items 1-3 for what's been resolved this sprint.

## Active Epics
- **Headless Preview CLI:** COMPLETED
  - Secondary binary target `preview_cli` built and tested. Supports interactive prompt fallback (`is_interactive = true`), drag-and-drop file/folder inputs, and automatic localized `previews/` folder generation.
- **Top Navigation & Functional Cancellation:** COMPLETED
  - Migrated vertical navigation to top bar, extracted Export Manager view, implemented non-destructive `INGESTION_CANCEL` thread interruption, and unified localized view footers.
- **Localization Infrastructure:** COMPLETED
  - Migrated hardcoded GUI/CLI/scanner strings to localizations. Updated `analysis::localization` to support transparent dual-key lookups (`#key` and `key`) for Valve KeyValues and AMXX files.
- **HLTV Active Frame Injection:** COMPLETED
  - Standalone `DRC_CMD_INEYE` frame injection implemented in `native/src/patch/engine.rs`.
- **Dynamic Drive Failover:** COMPLETED
  - AOT capture routing, duration math parity, JIT render routing, and UI/UX export pool indicators with dynamic vector list reordering.
- **Frontend Migration & Capture Studio Parity:** IN PROGRESS (Branch: `feature/tauri-migration`)
  - Transitioning frontend stack to Tauri + Vite architecture in `desktop-studio/`, restoring parity with legacy `dev` branch.

## IDE AI State
- **Current Goal:** All 5 Immediate Sprint Focus items resolved; now working through the `engineering_backlog.md` parity backlog (High/Medium/Low) before `feature/tauri-migration` is merge-ready. First High-priority item ("Custom Engine Commands Integration") done 2026-08-13.
- **Last Evaluated:** 2026-08-13
- **Status:** Immediate Sprint Focus items 1-5 all resolved. Remaining gaps cataloged in `engineering_backlog.md`.

### Immediate Sprint Focus (Top Priorities)
1. ~~**[CRITICAL BUG] Fix `capture_directories` Drive Mapping.**~~ **RESOLVED** — `capture_pane.js` now sources `capture_directories` from the Target Output Drives pool (falling back to Primary/Backup Media Dir, and toasting an error if neither is configured) instead of `state.scanPaths`.
2. ~~**[CRITICAL BUG] Implement `session_id` Timestamping.**~~ **RESOLVED** — `generateSessionId()` stamps `session_YYYYMMDD_HHMMSS` into the payload on every dispatch; `config_from_payload()` in `capture_manager.rs` now extracts `payload.session_id` into `PatcherConfig.session_id`.
3. ~~**[FEATURE] Global & Per-Demo Bookmark Previews.**~~ **RESOLVED** — Reverted the per-streak preview button. Added "Launch Preview" (per-demo, header area) and "Generate All Previews" (global, button group beside Select All/Deselect All) to `detail_pane.js`/`index.html`, both reactively disabled with zero selected highlights in their scope. Backend replaced `launch_live_preview` with `launch_demo_preview` (patches one bookmarked `<stem>_preview.dem` for the active demo, then launches HLAE directly via `+viewdemo <stem>_preview` — the old `primer_preview.dem` relay chain is gone) and `generate_all_previews` (patches a bookmarked preview per demo across the whole selection, no launch). Both share `patch_bookmark_previews()`, which reuses `build_preview_patch_jobs`' existing per-demo `svc_director` STUFFTEXT event injection (one event per selected highlight, sorted, plus MATCH_START/DEMO_END) as the "BOOKMARK" mechanism — no new director opcode was needed.
4. ~~**[UI FIX] Detail Pane & Formatting.**~~ **ACTUALLY RESOLVED 2026-08-13 — root cause found via screenshot, two real bugs fixed.** The two prior audit passes were checking the wrong layer: they verified the JS *join logic* (correct) and the localization *data* (also correct — every weapon key has a real value in `dod_tools_english.txt`), but not whether the app could actually **find** that data file at runtime. Two independent, confirmed root causes:
   - **Checkbox invisibility:** `styles.css`'s global reset (`button, input, select, textarea { appearance: none; ... }`) strips ALL native checkbox/radio rendering app-wide with zero replacement styling — the "Sel" checkbox (and every other checkbox in the app: POV Only, separate-HUD, auto-clear flags, audit-select-all, etc.) was present in the DOM and clickable but rendered as a blank, invisible hit-box. Fixed by exempting `input[type="checkbox"], input[type="radio"]` from the reset and giving them `accent-color: var(--primary-accent)` to match the theme.
   - **Garbled weapon/details text:** `analysis::localization::load_pass()` only checked `"localizations"`, `"../localizations"` (both CWD-relative) and one `.parent()` hop from the exe. From the Tauri app's actual runtime working directory (`desktop-studio/src-tauri/target/...`), none of those resolve to the workspace-root `localizations/` folder, so `translate_key("weapon.*")` silently returned `None` → `.unwrap_or_default()` → `""` for **every** weapon, permanently (the lookup result is cached process-wide after the first call). That's why single-kill streaks fell back to "N kills" (`timeline_string` was empty → falsy) and multi-kill streaks showed orphaned `(+m:ss)` fragments with no weapon name between them (the leading "." in the screenshot is a comma from `parts[0] = ""` rendered small at 0.8em). Fixed by walking up to 6 directories from the exe path looking for a `localizations/` folder instead of assuming a fixed depth — this is a real logic fix in application code, not a build-config change. The `weaponClean || 'Unknown'` defensive fallback added in the prior (inconclusive) pass now serves as a real safety net for this exact failure mode.
5. ~~**[UI POLISH] Tab Branding & Custom Controls.**~~ **RESOLVED** — Renamed the nav tab from "Workspace & Master Queue" to "Capture Studio" (`index.html`). Added a themed SVG chevron background to the global `select` rule in `styles.css` (the earlier `appearance: none` reset had stripped the native arrow with nothing to replace it); stripped the two inline `style="..."` selects (`#render-codec-select`, `.streak-status-select`) that would otherwise have overridden it via specificity, keeping only `.streak-status-select`'s dynamic per-status `color`. Removed the dead "POV Only" toggle entirely — `#config-hide-non-pov` input, label, and wrapper `.control-group` deleted from `index.html`; its unused `hideNonPovCheckbox` query deleted from `detail_pane.js`. The POV filter itself (`isHLTV`/`recPlayer` check in `renderDetailView`) never read that checkbox — it was already unconditional — so no downstream logic needed changing.

## Sprint Takeaways & Architectural Rules
* **Standalone CLI Portability:** Strictly avoid dynamic disk-based localization lookups for headless binaries (`preview_cli`). Hardcoded literals prevent silent failures on target machines missing dictionary files.
* **Drag-and-Drop Tokenization:** Windows Terminal and PowerShell format drag-and-drop paths. `stdin` parsers must explicitly strip the `& ` evaluation operator and handle single (`'`) and double (`"`) quote wrapping to prevent path fragmentation.
* **Immediate-Mode UI (egui) Consolidation:** Never silo shared data types (e.g., `.dem` queues) across separate UI tabs. Use state-driven UI swaps (e.g., `CaptureMode` toggle) within a unified workspace. Always wrap vertically expanding configuration panels in `egui::ScrollArea::vertical().id_salt(...)` to prevent off-screen clipping.
* **Egui Ownership Safety:** Chaining builder methods that consume `self` (like `Response::on_hover_text`) triggers `E0382` errors. Declare the response as mutable and reassign it before evaluating `.clicked()`.
* **AI Git Hygiene:** Always audit `git show --name-only HEAD` before pushing. Autonomous IDE agents can hallucinate commit messages and silently contaminate unrelated files.