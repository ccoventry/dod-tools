# Active Sprint State

> **Two independent work streams exist in this repo.** Everything below is the
> Capture/Render Studio (Tauri migration + capture-block-manifest) track. There is a
> second, unrelated stream — demo-derived match stats for the KTP league, living on
> `dev`/`main` only — tracked in `docs/demo_stats_feasibility.md`, not here. Check which
> one you're resuming before reading further.

## Current State (2026-08-26)

`dev`'s tip is `911604f`. Everything through PR #7 is merged. Merge chain, oldest to newest: `feature/tauri-migration` (2026-08-18) → `fix/dem-patch-delta-description-crash` (PR #1) → `chore/capture-allocation-cleanup` (PR #2) → `feature/capture-block-manifest` (PRs #3, #5) → `feature/capture-render-quick-wins` (PR #4) → `feature/ui-string-consolidation` (PR #6) → `fix/capture-disk-space-gates` (PR #7, unified the three disagreeing capture disk-space gates).

**In progress:**
- `chore/docs-cleanup-issues-migration` — docs/CLAUDE.md/CI reconciled, open items migrated to GitHub Issues. Not yet merged into `dev`.
- `feature/decal-flush-r-and-d` — decal hygiene between capture clips ([#60](https://github.com/ccoventry/dod-tools/issues/60)). **Verified in game 2026-08-26 — the feature works.** The ring sweep was validated in game (2026-08-24) and `m_Size` measured at ~4 units (a radius, so decals overlap only within ~8 units and a position may sit ~3 units off a surface). **Wiring** (`c9989cf`): `CaptureBlock` carries `record_start_tick`/`record_stop_tick`, `PatcherConfig` carries `decal_flush`/`decal_ring_limit`/`capture_fov`, `init_commands` pins `r_decals` last, and the clean runs as a pre-pass inside `StreamPatcher::patch` so its four call sites cannot drift; the injected-cvar frame stays off because it would shift every later frame ordinal by +1. **Tiling** (`f25853f`): positions tiled across planes fitted to real decals. **Camera-filter fixes** (`4aab914`): clearance became a ranking rather than a hard gate, and the pass no longer bails out when a demo has no settled spawn. **Map coordinate store** (`b886809`): proven world coordinates pooled per map build, keyed on name AND checksum, world decals only, exact values not grid-rounded; read-only seed dirs are supported so a shipped store could be distributed by an updater. **FOV fix** (`aa87cac`): the on-screen cone is derived from the capture FOV and frame shape rather than a fixed 40 degrees — at `mirv_fov 105` the frame corner is ~56 degrees off axis, so the old value was silently treating in-shot positions as hidden. **BSP geometry** (`a4bed14`, `1c86571`, `a9c9eab`, `da99dd7`): `patch::bsp` reads map surfaces, the node tree and visibility, and placement now asks whether a spot is genuinely hidden — PVS first, then the frame cone, then a line-of-sight trace — instead of asking only whether it falls outside the frame.

  **Measured 2026-08-26** across 85 demos spanning all 18 maps in the user's library, at FOV 105: **85/85 reach a full 68-position sweep, zero in-clip frames show a flush position, all decided from geometry.** Before the occlusion work three of those demos were getting a single position. The BSP parser is validated against 107,584 coordinates the engine provably accepted (98.93% land on a world face within 1 unit). End-to-end through the real capture path: frame ordinals preserved, one `r_decals` pin, no scratch left behind, ~6s of flush per demo.

  **Settings surface (2026-08-26, `30c3912`/`eb79a97`).** The sweep is sized to the decal ring, so the ring is one number and `r_decals` is where the engine reads it — `init_commands` is now the authority when it states it (last one winning, as the console does), the configured default fills in and gets pinned when it does not, and `r_decals 0` stands the flush down rather than pinning the cvar back up. Same rule `capture_fov` already followed for `mirv_fov`. `decal_flush` stays a separate switch because "capture the walls as the engine leaves them" is a different request from "keep no decals at all", and because a feature with an invisible failure mode needs an off switch that does not require a rebuild. New settings files seed `r_decals 256` and `mirv_fov 90` into init commands; existing ones are never appended to.

  **Map library verification (2026-08-26, `09e48a3`/`08c4e6b`/`c2b84b0`).** The map checksum a demo stamps in its header is computable from the map file: CRC-32 over lumps 1..14, entities excluded, left unfinalised (no closing XOR). Verified across the library — 397 of 397 first-person demos match their own map, 0 wrong, 0 missing; the 45 HLTV demos zero the field and are reported as unverifiable rather than called wrong. So a scanned demo can now say *missing* (unplayable) apart from *wrong build* (plays, and everything derived from its geometry is quietly wrong), which is the state nothing else in the app would ever mention. The flush refuses a mismatched map outright and falls back to the cone rather than reading a different world's faces, and HLTV harvests key off the local map's checksum instead of sharing one `_00000000` bucket. `map_fetch` downloads from the KTP mirror — scratch file first, checksum read from what actually arrived, and only then moved into place; an existing map is moved aside, never overwritten. `native/src/bin/check_maps` reports or fetches for a folder; the Master Queue shows a banner grouped by map.

  **Verified in game 2026-08-26.** `wsod25-po_r2_qf-m00cat_clinic_m2_lenn_h2.dem`, highlights 6/7/8/9 at kill range 1-2. The first two clips overlap in location, so the second would have shown the first's bullet holes had the flush not worked; instead the old holes were visible on the wall and vanished shortly before the capture began. An earlier run on `dod_railyard_s9a` also looked right, but that map's wall textures make decals hard to judge — lennon's are closer and more colourful, and lennon is where occlusion mattered most (`gorilla_soul_lenn_h1` went from 1 position to 68). Per the issue, this was the only test that catches this feature's failure modes.

  **The game's own configs set cvars the pipeline assumed it owned (2026-08-26, `5a3c7a5`).** The verified capture ran with no `mirv_fov` or `r_decals` in the app's init commands — but `config.cfg` ends with `exec movie.cfg`, and that file carries `mirv_fov "105"` and `r_decals "0"`. So the on-screen cone was sized for the default FOV 90 (~55 degrees) while the engine rendered at 105 (~62 degrees). It passed regardless — occlusion carries the decision, and the wider cone drops only 228 of 4,289 camera-safe candidates on that demo — but the assumption was wrong and invisible, which is this feature's signature failure. `patch::cfg_scan` follows the exec chain from the configs the engine runs on its own and reports what they set; FOV now resolves init commands → executed config → configured default. `r_decals` is deliberately *not* adopted the same way, since that same file sets it to 0 and the pipeline's own pin runs after the config anyway. Binds and aliases are not counted as settings — `bind "F7" "r_decals 4000"` is in that config, and the cvar there is a payload, not an execution. **The scanner is strictly read-only: nothing in this app writes, edits, moves or removes a config file.** `native/src/bin/check_cfgs` reports a folder; the Capture Config pane banners it above Init Commands.

  **Init commands that override a config (2026-08-26, `6dca217`).** Init commands run after the engine loads its configs, so where both name the same cvar the init command wins — which is the point of typing one, but it also means a config line the user set deliberately stops applying with nothing to say so. The scanner now records *every* assignment rather than only the cvars the pipeline reads, since most configs never mention `r_decals` and the collisions that surprise people are the ones nobody thought to watch for. An assignment is a cvar-shaped name with exactly one argument, which keeps `+mlook`, bare commands and `mirv_fov handleZoom enabled 1` out alongside the binds and aliases. The check runs against the commands a capture would really apply — `builder::final_init_commands` is extracted so the app's own additions count too, which on the real folder catches `mirv_movie_fps 120` displacing `movie.cfg`'s 300. That also settles the seeded defaults: `r_decals 256` and `mirv_fov 90` can stay, because a new user now sees exactly what they displace.

  **Commands that never apply (2026-08-26, `b356d9d`).** `final_init_commands` puts the user's entries first and the pipeline's own after them, and the last one wins — so `mirv_movie_fps 500` typed by hand is dead on arrival against the `mirv_movie_fps <capture_fps>` appended a few entries later, with both lines visible and nothing to separate them. `cfg_scan::self_overrides` reports those, naming the setting that owns the value (editing the line cannot win, so pointing at the line would be useless advice). Commands that never apply are dropped from the config-override list rather than reported twice with opposite implications.

  **Custom commands, and the one that breaks the feature (2026-08-26, `c670d95`).** Custom commands are scheduled into playback, so they run after the game's configs *and* after the init commands — the last word on anything they set, and the only place a cvar changes partway through a capture. One case is not merely surprising: **`r_decals` scheduled mid-demo breaks the flush.** The cvar bounds how far the decal ring may travel before it wraps and evicts nothing, so lowering it during playback strands every decal above the new limit for the rest of the demo, while the capture completes and looks plausible. `cfg_scan::MID_DEMO_HAZARDS` names it, and the banner gives it its own section, first, saying to set the ring once in Init Commands instead. The pipeline has never scheduled `r_decals` itself and has a test saying so — nothing stopped a person doing it. Also deduplicated the override list to one row per cvar: typing a value the app also appends produced two rows with the identical consequence.

  **Labels (2026-08-26).** The two command lists on the Custom Commands tab are now **Initial Commands (run once at demo load)** and **Scheduled Commands (run relative to each highlight)** — both lists are custom, so "Custom Commands" named nothing; when they run is the actual difference. Same verb on both, so the parenthetical carries the distinction. The tab keeps its name as the umbrella.

  **Regression sweeps after the settings/map/config work (2026-08-26).** Re-ran the full 85-demo sample at ring 256 and again at ring 4096 with every change from today in place. **Both identical to their pre-change baselines, demo for demo:** 85/85 full sweeps, zero in-clip frames showing a flush position, all decided from geometry — 5,780 positions at ring 256 and 87,380 at 4096, with 1 short burst of 668 at 4096 (a 37-records/sec HLTV demo, unchanged from before). End-to-end through the real capture path: ordinals match, one `r_decals 256` pin, no scratch left behind, and the FOV now resolved from the user's `movie.cfg` as intended.

  **The flush lead is a duration now (`8ebc216`), and needs a re-test.** It was a flat `lead_ticks: 300` — a frame count standing in for a duration, which `docs/app_architecture.md` already forbids under **"Pure Float Timestamps"** and which `docs/bugs.md` records as the root of *"All injected commands collapse to Tick 0"* (the patcher comparing gameplay ticks against the raw packet index, "which grew exponentially faster than the actual game time"). **Measured across the 85-demo sample (`demo_tickrate`): the flat 300 was worth a median 0.60s, minimum 0.44s — 77 of 85 demos under 1.0s**, because 74 run above 410 frame records/sec and a "tick" is a parsed frame record (~4.4x the header's `frame_count`). Now `lead_seconds: 2.0`, resolved by `deadline_before` walking the frames' own `.time` values — **not** `seconds * demo_fps`, which would repeat the same mistake in a smaller way. The walk stays inside the window's own directory entry, since timestamps restart per entry and a LOADING entry's near-zero stamps would drag the deadline to the demo's front; it falls back to the old 300 frames when the entry cannot answer. 2.0s is more margin than the ~0.6 that passed in game, comfortably inside a default pre-roll so the burst cannot reach an earlier clip, and room for the engine to ingest a maximum sweep's 4,112 decals rather than finishing just in time. New stat `burst_frames_inside_clip` counts carriers landing inside a clip's own record window — turning the ring mid-shot could evict the bullet holes the firefight is putting up — and measures **0** at both ring sizes. Design for what comes after is in `docs/decal_flush_bsp_surfaces.md` — BSP-derived coordinates (stage 3) and an adaptive maximum sweep that would retire the `r_decals` pin entirely (stage 6, now reachable: every demo has ≥3,916 camera-safe candidates against the 1,028 it needs). Diagnostics: `native/src/bin/validate_bsp`, `survey_decal_flush`, `verify_decal_pipeline`. Note for testing: `monday-wsod25_r07_m1*` and `_m3*` demos are corrupt (`_m2` is fine).
  **Re-verified in game, 2026-08-26 (second run, full batch).** Same demo, now with the 2s duration lead and the Select All fix in place — the previous attempt had loaded the demo three times because Select All ignored the visible-streak filter and picked every player's highlights, producing five chain demos from one demo. This run produced **one `chain_01.dem`, 15 blocks, 15/15 takes on disk and renderable, no reload, no junctions left behind.** The flush stripped 1,115 wall decals and 101 sprays outside the 15 clips and injected 4,080 across **68 of 68** positions in 15 bursts, all geometry-decided, with neither the partial-sweep nor the in-clip-burst warning firing. Timings audited against the demo's real 392.4 records/sec: pre-roll 5.00s, sound flush 1.00s, post-roll 1.00s, and the user's Before command landing at `SPEED_FLUSH + 1` — at the fast-forward exit, never inside it. Every take's `sound.wav` duration matches its frame count exactly.

  **`SZ_GetSpace: overflow on netchan->message` is not the flush** — settled without an A/B run, because `qconsole.log` turned out to be append-only across sessions (see below) and therefore a comparative record. The rate holds at ~1 per 60,000 frame records in *every* session in that file including ones predating the flush entirely, and six of the eight in the audited run landed mid-fast-forward nowhere near an injection. Recorded in `docs/goldsrc_dod_quirks.md` and as a caveat on the existing `svc_bad` entry in `docs/bugs.md` so it does not get re-investigated.

  **Two bugs found by reading that log (`8f4599c`, `6089214`).** `_route_N` junctions were never unlinked: `build_batch_queue` creates them but returns long before a batch ends, so the only thing that ever removed one was the next batch reusing that index — `_route_0` had been sitting in the game folder indefinitely. They are registered with `WorkspaceGuard` alongside `dod_pool_N` now. And `auto_clear_logs` named `dod/qconsole.log`, but `-condebug` writes beside `hl.exe` because HLAE's custom loader leaves the working directory at the game root; the path had never matched anything, silently, and the log had accumulated 34 sessions. (Which is what made the `SZ_GetSpace` question answerable — worth keeping, and the user leaves the checkbox unchecked deliberately.)

  **The pin is spent at the 4096 ceiling too when a config sets `r_decals` (`6a2d1e1`).** At `decal_ring_limit = MAX_RENDER_DECALS` the pin was skipped, on the grounds that a maximum sweep turns a full revolution whatever the cvar happens to be. That holds for every value but zero — and `r_decals 0` in a `movie.cfg` is in the working install, so the sweep would have been sized to the maximum, reported a full revolution, and injected into a ring the engine keeps nothing in, with nothing in any logged statistic to show for it. `ring_set_by_game_config` asks `cfg_scan`; the config decides *whether* to pin, never *what* to pin to. It also makes the config warning honest, since it already claimed the pin always overrides. **Currently dormant:** `decal_ring_limit` is a default filler, not a UI knob — no frontend code sends it and `settings.json` does not persist it — so `r_decals` in Init Commands is the only way to change the ring, which is the intended design (one number, and `r_decals` is where the engine reads it). Found while writing the tests: `mock_config` named the game folder rather than `hl.exe`, so every test in the file shared a single `%TEMP%/dod` and deleted each other's `.cfg` files mid-write under parallelism.

  **The camera test was judging the wrong point (2026-08-27, `b80eec3`) — found by raising `r_decals` to 4096.** Grids of flush decals appeared on walls in plain view in two takes, while the log reported zero on-camera frames. `R_DecalShoot` does not draw at the coordinate it is given; it projects onto the nearest surface. So a coordinate inside a wall passes "is it in shot with nothing solid in the way" *perfectly, from every camera, always* — it is occluded by the very surface it renders on. The filter therefore did not miss those candidates, it **preferred** them: the deeper inside a wall, the safer they scored. Measured against every in-clip camera sample rather than the usual stride of four, on the demo that showed it:

  | ring | positions | inside solid | drawn face in shot |
  |---|---|---|---|
  | 256 | 68 | 0 | 0 |
  | 1024 | 260 | 62 | 53 |
  | 2048 | 516 | 231 | 151 |
  | 4096 | 1028 | **662** | **437** |

  Candidates now resolve through `bsp::decal_draw_point` — projected onto the nearest world face and lifted clear of it — and the cone, the trace and the on-camera count all judge that. Coordinates in a solid leaf are dropped outright, and so are ones with no face inside the engine's own ~3-unit slack: tiling lays a grid across a fitted plane, the plane runs on past the brush that proved it, and tiles landing in open air were being counted into a sweep they could not turn. **After: 0 drawn faces in shot at every ring.** 256 and 1024 still reach a full sweep on that demo; 4096 reaches 736 of 1028 and now says so.

  **The distance escape was scoring far positions SAFE, not merely skipping them (`b86b835`).** `visibility_max_distance` was 1800 units, on the reasoning that one decal that far away is not readable on screen. The flush does not place one decal — it places a grid, four to a position, and a grid reads where a dot does not. The cutoff made `on_screen_from` return "not on screen", so anything past it was certified hidden: on the demo above, four chosen positions had a camera with an unobstructed view of them, nearest at 1,848 units. Now `INFINITY`, which cost no candidates there and about six seconds of tracing.

  **Library sweeps, 2026-08-27, all 427 demos in the Steam folder** (not the earlier 85-demo sample):
  - **ring 256, final code: 427/427 full 68-position sweeps, zero on-camera, all geometry.** The shipped default is unaffected by any of the above.
  - **ring 1024: 427/427 full 260-position sweeps, zero on-camera, all geometry.** Every demo reached exactly 260.
  - **ring 4096: 427/427 full 1028-position sweeps, zero on-camera, all geometry.** Every demo reached exactly 1028, and only one needed anything beyond the tiled source to get there (tiled-safe minimum 861, median 10,403).
  - ring 2048: 27/27 when the run was stopped to free the machine.

  **VERIFIED IN GAME 2026-08-27 at ring 4096.** Same demo, all 15 highlights, `r_decals 4096` in Init Commands. **The two grids that exposed the bug are gone — the user watched both locations across every clip and saw neither.** Full sweep (1028 of 1028 positions, 15 bursts, 61,680 decals), 15/15 takes renderable, 407.7s, no junctions left behind.

  The capture is otherwise **byte-for-byte the same run** as the clean ring-256 capture two nights earlier: identical marker ticks (`SPEED_FLUSH 17593`, `AUDIO_SYNC 19163`, `START_RECORD 19555`, `STOP_RECORD 23479`), identical take durations to the hundredth of a second (426.89s total against 426.88s), and the same eight `SZ_GetSpace` lines in the same places. So placing **15x** the decals (61,680 against 4,080) moved nothing about timing, frame ordinals, take structure, or the netchan drip — which independently confirms the `SZ_GetSpace` finding rather than merely restating it.

  **Caveat on the 4096 result: it was measured against a mature coordinate store.** A map the store has never seen starts sparse, and the achievable ring rises as captures harvest it. That is not a hidden failure — the partial-sweep warning already names the shortfall and recommends the ring a demo can actually turn — but "4096 works" is a statement about this library's 17 populated maps, not about a fresh install or a new map.

  **The coordinate store is what decides how high the ring can go, and running the surveys filled it.** `dod_lennon2` went from 2,592 coordinates to ~14,127 and the store went from 4 maps to 17, because a survey harvests exactly as a capture does. On the demo above that turned a 736-of-1028 shortfall at ring 4096 into a **full 1028-position sweep, zero inside solid, zero drawn faces in shot** — so stage 3 is no longer a prerequisite for a maximum ring, and the earlier "needs BSP-derived coordinates first" reading was supply, not structure. It also means tonight's surveys are **not independent of each other**: each ran against a richer store than the last, so supply figures are not comparable across runs. The safety figures are unaffected.

  **Unrendered faces do allocate ring slots — the stage 3 prerequisite is answered (2026-08-27, in game).** Run behind `DOD_FLUSH_PVS_ONLY`, which restricts placement to positions whose drawn surface is outside the union of the capture's camera PVS — faces the engine provably never renders. Same demo, same 15 highlights, ring 256: **68 of 68 positions, 4080 decals, 15 bursts, 15/15 takes renderable, 404.7s, no old decals surviving into any clip.** qconsole for the session is clean — 15 `START_RECORD` / 15 `STOP_RECORD`, no `svc_bad`, 8 `SZ_GetSpace` (the usual drip, same count as the 4096 run). So `R_DecalShoot` takes its slot at shoot time and rendering is a separate later pass; see the ring bullet in `docs/goldsrc_dod_quirks.md`. **Consequence for stage 3:** candidate coordinates no longer need to come from harvested demos at all — any BSP face in a leaf outside the camera PVS union is a valid dump site, so a new map can get a full sweep from a BSP scan alone. What this run did *not* test is supply: it turned a 68-position ring against a 14,128-coordinate store, so whether a BSP scan yields 1,028 PVS-hidden positions on an arbitrary map is still open.

  **Stage 3 built — flush candidates sampled off the map's own faces (2026-08-27).**
  `Bsp::face_candidates` lays a 32-unit grid across every world face, inset 8 units from the
  polygon edges and lifted 2 units along the outward normal, largest faces first, capped at 48 per
  face and 12,000 overall. Samples landing inside solid are dropped at the source — 0.5% to 21% of
  raw samples do, on faces sealed against another brush or facing the void, and each one is the
  projection bug waiting to happen. Supply across **all 219 installed maps**: every map yields at
  least 6,561 candidates (mean 11,835) against the 1,028 a 4096 ring needs, 100% projection hit
  rate, ~15 ms to scan. Measure it with `native/src/bin/probe_map_candidates`.

  **What it is worth in a capture.** A/B over a 64-demo sample at **ring 4096 with no coordinate
  store** — the new-map case, and the only configuration that stretches the proven sources: full
  sweeps went **57/60 → 60/60**, and the smallest sweep any demo got went **293 → 1,028**, with
  zero positions on camera either way. The map source fired on 16 of 60, supplying a mean of 7,210
  camera-safe candidates (min 4,832) against the 1,028 needed. `monday-wsod25_r07_m3_h1.dem` laid
  55 tiles and kept 20; `probe_fine_in.dem` kept **zero** of its 76 — every one in shot — and the
  map carried both to a full sweep on its own. **At ring 256 the change is invisible** (60/60
  before and after), which is the intended shape.

  Wired as a **fallback**, reached only when the demo's own decals and the coordinate store
  together cannot fill a sweep, so a mature map pays nothing for it and the validated ordering is
  untouched. `DOD_FLUSH_MAP_GEOMETRY_ONLY=1` forces it — the only way to put the new source in
  front of someone watching the game, since on this library it otherwise almost never fires.
  **The artefact to watch for on that run is a grid of decals visible *through* something**: the
  hull trace treats a fence or grate brush as solid while it renders see-through, and the map
  source is the first one that deliberately reaches behind them. Pre-existing mechanism, higher
  exposure. Stage 5 (writing map-derived coordinates into the store) is deliberately **not** done:
  the store's invariant today is "coordinates the engine accepted", which is what `validate_bsp`
  tests against, and mixing computed coordinates in under the same key would retire that check.

  **Ring pre-check for the next in-game test.** `survey_decal_flush` with `FLUSH_RING` at 256 / 1024 / 2048 / 4096 on the lennon demo: full sweep at every level (68 / 260 / 516 / 1028 positions, wanted == got), zero on-camera, all geometry, drawn from 8,934 camera-safe tiles of 27,523 laid. The only untested variable at 4096 is how the engine ingests 4,112 decals per burst rather than 272.

Test demos and screenshots now live in `local/demos/` and `local/screenshots/`, both gitignored via `/local/`. Repo-relative paths were repointed to match in PR #61 (`chore/demos-into-local`, merged into `dev` 2026-08-25).

**Next work:** pick from the open GitHub Issues, or whatever the user brings to a fresh session.

**Local cleanup outstanding:** a stray local-only branch `audit/capture-render-ux` — its one unique doc was cherry-picked over to `fix/capture-disk-space-gates` before that merged, the branch itself was never deleted (`git branch -d audit/capture-render-ux`), harmless but unused.

Full narrative history through 2026-08-23 (the detailed day-by-day bug hunts, phase completions, etc.) has been moved to `docs/sprint_history_archive.md` — this file was getting too large to serve its actual purpose (a quick landing point for a fresh session). Check `engineering_backlog.md` for anything still open.

## Sprint Takeaways & Architectural Rules
* **Standalone CLI Portability:** Strictly avoid dynamic disk-based localization lookups for headless binaries (`preview_cli`). Hardcoded literals prevent silent failures on target machines missing dictionary files.
* **Drag-and-Drop Tokenization:** Windows Terminal and PowerShell format drag-and-drop paths. `stdin` parsers must explicitly strip the `& ` evaluation operator and handle single (`'`) and double (`"`) quote wrapping to prevent path fragmentation.
* **Immediate-Mode UI (egui) Consolidation:** Never silo shared data types (e.g., `.dem` queues) across separate UI tabs. Use state-driven UI swaps (e.g., `CaptureMode` toggle) within a unified workspace. Always wrap vertically expanding configuration panels in `egui::ScrollArea::vertical().id_salt(...)` to prevent off-screen clipping.
* **Egui Ownership Safety:** Chaining builder methods that consume `self` (like `Response::on_hover_text`) triggers `E0382` errors. Declare the response as mutable and reassign it before evaluating `.clicked()`.
* **AI Git Hygiene:** Always audit `git show --name-only HEAD` before pushing. Autonomous IDE agents can hallucinate commit messages and silently contaminate unrelated files.
