# Engineering Backlog

## 📋 Immediate Tasks & Critical Bugs (Current Sprint)

### ✅ Unify the three independent, disagreeing capture disk-space gates (branch `fix/capture-disk-space-gates`, scoped 2026-08-23, fixed 2026-08-23)
Full writeup was Section 1, Item 2 of `docs/capture-render-ux-audit.md` — now fixed, see that doc's status header for which of its other findings are still open.

**The problem**: three separate, non-shared implementations of "is there enough disk space for this capture batch," which can disagree with each other:
1. **JS pre-flight** — `computeRequiredCaptureBytes` (`desktop-studio/src/capture_pane.js:63-121`), decides whether the Start button is enabled.
2. **Rust AOT allocation** — `allocate_blocks_first_fit_decreasing` + `block_estimates` (`native/src/patch/builder.rs:500-520`), snapshots free space once per drive at batch-build time and routes each block.
3. **Rust pre-launch abort** — `capture_engine.rs` (~line 397, confirmed still present), re-queries disk space with its own separate `sysinfo` call right before spawning HLAE and hard-aborts if the *primary* export dir alone has under a hardcoded 15GB free — regardless of what #2 already decided about the whole drive pool.

Only #3 re-checks space at the moment it actually matters (immediately before HLAE launches), but it's the narrowest of the three (single primary drive only). A batch can pass #1 and #2, then hard-abort seconds later from #3 — or the reverse, where #3 passes while #2 silently ran out of room on a secondary drive earlier in the pipeline.

**Proposed fix (from the audit)**: delete `capture_engine.rs`'s standalone 15GB check entirely; have `build_batch_queue` return the final per-drive headroom it already computed, so the pre-launch step re-validates *those* same numbers instead of computing a third, narrower answer. The JS pre-flight estimate doesn't need bit-for-bit parity with the Rust allocator (it's only ever a UX guard), but ideally keys off shared constants rather than an independent literal.

**Why this one, not the audit's other 8 findings**: flagged as the highest real-world-impact item — a correctness/reliability issue that can actually surface during real capture batches, not just a UX-polish or code-cleanup item (unlike most of the other 8 findings in the same doc, which remain untouched/available for a future pass). Low risk to fix: disk-space bookkeeping only, doesn't touch frame injection, the Cbuf limit, or process lifecycle — no CLAUDE.md guardrail directly implicated.

**Fixed, per the proposed-fix shape above**: `capture_engine.rs`'s standalone `sysinfo`-based 15GB primary-dir check is gone. `build_batch_queue` (`native/src/patch/builder.rs`) now returns `(Vec<PatchJob>, Vec<(PathBuf, u64)>)` — the second element is the final post-allocation headroom for every drive the batch actually touches: every drive that got at least one capture block routed to it (`utilized_drives`), plus drive 0 unconditionally, since the primer + each job's demo file always land on `capture_directories.first()` regardless of block routing. `spawn_capture_engine` takes that vector as a new parameter and its pre-launch check is now just a loop comparing each entry against the threshold — re-validating the exact numbers #2 already computed instead of recomputing a third, narrower answer via a fresh `sysinfo` call scoped to the primary dir alone. The two previously-independent 15GB literals (`builder.rs`'s `FAILOVER_THRESHOLD` and `capture_engine.rs`'s inline constant) are now one `pub const MIN_DRIVE_HEADROOM_BYTES` in `native/src/sys/disk.rs`, next to the `get_available_bytes`/`diagnose_path` functions that already do the per-drive disk querying. `capture_manager.rs` threads the returned headroom vector from `build_batch_queue` straight into `spawn_capture_engine`. JS pre-flight (`computeRequiredCaptureBytes`) left untouched, per the audit's own scoping — it's a UX guard only and never had an independent threshold literal to unify.

Updated all call sites for `build_batch_queue`'s new return shape (`builder.rs` unit tests, `native/src/bin/check_ticks.rs`, `native/src/bin/test_builder.rs`). `cargo build --workspace` and `cargo test --workspace --no-fail-fast` both clean — 27/27 in `native`'s own suite (including all pre-existing `patch::builder` tests), zero failures anywhere in the workspace.

Not done, deliberately out of scope per the audit's own "(c) Proposed fix" note: true cross-language parity between the JS estimate and the Rust allocator's merge-billing algorithm — flagged there as "real work" the JS-only UX guard doesn't strictly need.

*(Follow-up gaps found while live-testing the fix above — the drive-headroom threshold split, the hl.exe-install-drive check, the patched-demo persistence model, and the stale Capture Output footer — are now tracked as GitHub Issues: [#8](https://github.com/ccoventry/dod-tools/issues/8), [#11](https://github.com/ccoventry/dod-tools/issues/11), [#12](https://github.com/ccoventry/dod-tools/issues/12), [#13](https://github.com/ccoventry/dod-tools/issues/13).)*

### ✅ Frontend string-consolidation branch (`feature/ui-string-consolidation`, PR #6) — manually verified, zero issues found (2026-08-23)
Every user-facing frontend string was moved into `desktop-studio/src/strings.js` across 19 commits (later 22 after backlog-doc follow-ups) — see the "Frontend String Centralization" entry in the General Backlog section below for the full architecture and the three follow-ups it surfaced. After 3 rounds of automated/agent-driven gap-audit sweeps found and fixed everything the static checks could catch, ran a live manual QA pass against the actual running app (`npm run tauri dev`) using a new checklist, `docs/qa_checklists/string-consolidation.json` (4 priority tiers: Analyzer tab, Highlight Details, multi-argument interpolated strings, static labels/headers). Went through it screenshot-by-screenshot against real demos — confirmed correct: team-name caps and Steam ID formatting on Player Details, all 4 stat card badges, Kill Streaks weapon-category filter buttons and delta/duration formatting, the Scoreboard's `>`/`=`/`<` comparison glyph actually matching which team was ahead, Team Score Timeline's tooltip correctly pairing team label/color with the right series, Rounds' duration formatting, Chat Log's team filter across several filter-combination scenarios, the Highlight Details status dropdown and weapon-chain text, tracked-badge tooltips dynamically listing the correct subset of reasons, Clear Selected/Clear All modal counts, Demo Auditor's group/file counts, and every static nav/Capture-Config/Render-Studio/Auditor label. Zero display bugs found anywhere. One thing clarified, not fixed: the "Remove Tracked Demo"/Clear Selected/Clear All modal's `(Captured/Rendered status, a note, or an edited kill range)` text is a generic explanation of the term "tracked work," not a per-demo reason breakdown — confirmed as original, correct, pre-existing design (the tooltip on an individual demo's badge *is* precise; the bulk-action modal was never meant to be). PR #6 (`feature/ui-string-consolidation` → `dev`) is ready to merge.

### ✅ Disk-space checks trusted a mount-point prefix match with no check the actual path was usable — affects real capture/render drive allocation, not just the UI (found + fixed 2026-08-23)
User caught this testing the Capture Output yellow-warning feature from earlier tonight: added one valid Capture Output path, then appended `|` to the end and blurred. The warning banner correctly flagged the path as `malformed` (the new `diagnose_capture_output_paths` code working right), but the footer's "Capture Output Free" number didn't change at all, and Start Capture Batch stayed enabled — user correctly guessed it might be "evaluating the drive itself and not the path." Confirmed: `get_available_bytes` (`native/src/sys/disk.rs`) never checked whether the requested path actually resolves to anything — it just string-prefix-matches the path against known disk mount points (`"c:/real/folder|garbage".starts_with("c:/")` → true) and returns that whole drive's total free space if any prefix matches, regardless of what comes after. A malformed suffix like `|` doesn't break the prefix match at all, so the function happily reported real C: drive space for a path that can never actually be written to.

This isn't just a UI-preview bug — `get_available_bytes` is the same function `native/src/patch/builder.rs` uses for **real capture drive-allocation routing** (`build_batch_queue`'s bin-packing) and `native/src/hlcr/renderer.rs` uses for **real render JIT export routing**. A malformed-but-prefix-matching configured path could have been silently treated as a valid allocation target in an actual capture/render batch, not just in this warning banner. **Fixed**: `get_available_bytes` now calls the same `diagnose_path` used by the Capture Output diagnostics, and rejects (`u64::MAX`, same "excluded" sentinel as no mount match) any path classified `Malformed`/`NotAbsolute`/`NotADirectory` before ever trusting the mount-point match — a `NotFound` path still passes through unchanged, since many capture/render output folders are legitimately auto-created at write time and "doesn't exist yet" alone isn't disqualifying. The check runs once per the existing 2-second TTL cache refresh (not on every call), so it doesn't add extra per-call cost to a function used in hot allocation paths. **Re-verified live (2026-08-23)**: pipe-suffixed path → 0.0 GB free, red blocking error; removed the pipe → back to 1019.7 GB, no warning. Fully resolved.

**Follow-up, same session, user-requested**: fixing the above surfaced a second, smaller inconsistency — a well-formed path whose parent drive is real but the leaf folder doesn't exist yet (e.g. `...\02_HLAE_Rawww`, a typo'd-but-plausible folder) still got listed in the yellow "won't be used" warning even though `get_available_bytes`/the footer already correctly counted it as usable (not-yet-created is fine, per the fix above) — two signals disagreeing about the same single entry. Fixed by making `diagnose_capture_output_paths` return a `usable: bool` computed via the *same* `get_available_bytes` call the footer uses, instead of deriving a second opinion from `status` alone; the frontend's problem-filter now keys off `usable`, not raw status. **Also added, per user request**: a calm blue informational note (distinct from the yellow/red warnings) for exactly this case — "`\"path\"` doesn't exist yet — it'll be created when the capture starts" — since silently treating it as fully fine risked masking a genuine typo of an *intended* existing folder. Verified live: blue note shown correctly, footer still reads the real free-space total.

### ✅ Render-recovery prompt fired on every F5 reload during an active render, even though nothing was interrupted (found + fixed 2026-08-23)
Found incidentally while retesting Section 5's NVENC concurrency warning: pressed F5 mid-render (to double check a toast), and the "Render Batch Interrupted" recovery modal popped up — not because anything crashed, but because `check_render_autosave` (`render_manager.rs:651`) only ever checked whether `.render_autosave.json` exists on disk, with no idea whether a render is actually still running in this process. F5 reloads the WebView only, not the Rust backend — the render job the user was watching kept running the whole time, untouched. The autosave file is written continuously as each job finishes (not just at batch end), so it's legitimately present during a real, ongoing render, which is exactly when this false-positive prompt fired. Clicking "Discard" didn't stop anything either (it only deletes that file; the detached render task doesn't check it), and the prompt came back on a third F5 because the next job finishing simply rewrote the file. **Fixed**: `check_render_autosave` now takes `state: tauri::State<'_, RenderManager>` and returns `None` immediately if `state.is_rendering.load(Ordering::SeqCst)` is true — a render actually running in this process is never something to "recover," it's just still going. **Re-verified live (2026-08-23)**: started a render batch, pressed F5 mid-render — no recovery prompt appeared, and Render Studio's job table still showed both jobs actively rendering after the reload. Fully resolved.

### ✅ Closing the app while an Init/Custom Command field still had focus silently dropped the edit (found + fixed 2026-08-23, confirming a risk the testing checklist had flagged but not yet verified)
`capture-render-quick-wins-testing-checklist.md`'s Section 4 specifically called this out as an open question when it was written: `list_editor.js` (backing Capture Output, Render Folders, Render Studio's Export Drives, Init Commands, and Custom Commands) updates its in-memory item array on every keystroke (`'input'`), but only triggers the actual `persistAppSettings()` disk write on `'change'` (blur/Enter) — deliberately, to avoid a full settings write per character while composing a value. The checklist noted: "closing the app while a field still has focus... may not be persisted... confirm whether this matters in practice." User reproduced it directly: edited a Custom Commands row, closed the app via the window's X button without clicking away first, reopened — the edit was gone. **Confirmed it matters, so fixed**: `main.js` now registers `getCurrentWindow().onCloseRequested(...)`, unconditionally `preventDefault()`s the close, awaits one final `persistAppSettings()` (which reads the already-current in-memory state — the edit was never lost from memory, only from disk), then explicitly `destroy()`s the window itself. Matches the documented Tauri v2 pattern exactly (`node_modules/@tauri-apps/api/window.js`'s own `onCloseRequested` doc example). **Re-verified live (2026-08-23)**: first retest hit a second real bug — `window.destroy` isn't covered by `core:default`, so the fix's own `appWindow.destroy()` call was rejected with a permissions error, which meant the app couldn't be closed via the X button *at all* until fixed (confirmed via the generated `desktop-schema.json`: `core:window:allow-destroy`, added to `desktop-studio/src-tauri/capabilities/default.json` — same "not in `:default`, needs explicit grant" pattern as the `dialog:allow-confirm` gap from an earlier session). After that fix and a rebuild, re-verified directly against `settings.json` on disk (not just the UI) rather than relying on a screenshot: typed `"adding without leaving the box"` into Init Commands, closed via the window's X without blurring, and `%APPDATA%/dod-tools/settings.json`'s `init_commands` array contained exactly that string. Fully resolved.

### ✅ Capture Output's "unusable pool" warning gave the same generic message for an empty list and for a list full of invalid paths (found + fixed 2026-08-23)
Found while testing Section 3 of `capture-render-quick-wins-testing-checklist.md`, which turned out to reference a UI field ("Media Output Directory") removed back on 2026-08-17 — the real equivalent today is the mandatory "Capture Output" list. While confirming that, added an entry of just `"a"` and got the same "No Capture Output directories configured" message as an empty list — misleading, since a directory *was* configured, it just didn't resolve to anything usable. Root cause: `refreshLaunchGuard` (`capture_pane.js`) computed `noDrivesConfigured = effectiveDrivePool.length === 0 || availableBytes === 0` — one merged condition for two different problems. **Fixed** with a real per-path diagnosis, not a guess: added `native::sys::disk::diagnose_path` (gated non-wasm, mirrors `get_available_bytes`'s cfg pattern) classifying each path as `Ok` / `NotAbsolute` (no drive letter or UNC root — Rust's `Path::is_absolute()`) / `Malformed` (OS rejected the syntax — `ERROR_INVALID_NAME`/`ERROR_INVALID_PARAMETER`/`ERROR_BAD_PATHNAME` from `fs::metadata`'s raw OS error) / `NotFound` (well-formed, doesn't exist, or an unmounted drive letter) / `NotADirectory` (exists, but is a file). New `diagnose_capture_output_paths` Tauri command surfaces this per-path; the frontend only calls it once the aggregate sum is already known to be zero (not on every keystroke — a per-path OS stat isn't as cheap as the existing TTL-cached aggregate sum). Verified each category individually: `Z:\` (unmounted drive) → `not_found`; `"a"`/`"b"`/`"c"` (no drive root) → `not_absolute`, joined into one message when multiple entries have problems.

Also chased what looked like a second, much scarier bug on the same list — a long mix of invalid paths (`C:\test|pipe`, `C:\test"quote`, `C:\test<>folder`, etc.) stopped showing any warning at all and let Start Capture Batch go enabled, surviving a full `tauri dev` restart (ruling out HMR staleness). Turned out **not to be a bug**: the footer's "Capture Output Free" read a real 4080.9 GB, meaning one of the original real drives (`C:\DICEWSOD25\...` etc.) was still in the list, uncleared, further down than the visible scroll position. The aggregate gate is deliberately "does *any* configured drive have room" — one valid entry correctly clears the warning for the whole pool, by design, since real usage means several real drives configured at once. Confirmed the per-path diagnostic fix above is unaffected by this — it was never broken, just correctly not firing once a genuinely usable drive was mixed into the pool.

**Follow-up UX pass, same session, user-requested**: that "at least one drive usable" case was silently swallowing the bad entries' problems entirely — nothing told you they'd be skipped. Added a non-blocking **yellow** banner (`#ffa726`) for exactly that case (`hasPartialProblems` in `refreshLaunchGuard`), reusing the same per-path diagnostics already fetched for the red case rather than a second IPC round-trip. Also capped the reason list at 3 entries (`+N more` tail) and switched from a semicolon-joined sentence to a point-form bullet list (`white-space: pre-line` on `#disk-space-warning-banner`, still plain `textContent` throughout — no `innerHTML`, so user-typed path text can't be interpreted as markup) after the first version proved to be an unreadable wall of text with a long invalid list. Confirmed working via screenshot: yellow bullets, capped at 3 + "...and 7 more", Start Capture Batch stays enabled.

### ✅ Render job failures never wrote anything to the activity log (found + fixed 2026-08-23, same testing session as the reveal-in-explorer bug below)
Noticed comparing two back-to-back testing sessions in the same `activity_20260822.md`: repeated `reset_render_job N (was Some("Error")) -> Queued` / `resumed the scheduler` / `scheduler loop exiting` triplets with zero indication anywhere in the file of *why* each attempt failed — the actual reason (missing take folder, in this case) was only ever visible via the UI's per-row "View Log" button. Root cause: `RenderUpdate::Finished`'s handler (`render_manager.rs:295-344`) only calls `log_markdown` inside the `if success` branch (the existing `[render-take-finished]` line) — every failure path in `native/src/hlcr/renderer.rs` (missing take folder, missing audio file, FFmpeg not found, JIT drive-routing failure, output-folder creation failure, FFmpeg spawn/exit/wait failures, user cancellation) sends `Finished(id, false, Some(error_msg))` but that message never reached the log. **Fixed**: added a parallel `just_failed` capture alongside the existing `just_finished`, logging a `[render-take-failed] job {id} base_name={...} error={...}` line whenever a job finishes unsuccessfully. Verified via a fresh reset-to-error cycle — the new session in the same log file now shows both `error=Cancelled by user` and `error=Take folder not found: ...` lines exactly where the old session had silent gaps.

### ✅ "Open Take Folder"/"Open Output" silently opened the wrong folder on a missing path (found + fixed 2026-08-23, testing `capture-render-quick-wins-testing-checklist.md`)
Found while running Section 1's edge case ("delete/rename a take folder, click Open Take Folder — should toast an error, not crash"). Renaming the folder didn't crash, but it also didn't error — the button silently opened Explorer to whatever its configured default location is (OneDrive - Personal, in this case), giving no indication anything was wrong. Root cause: `reveal_in_explorer_impl` (`desktop-studio/src-tauri/src/audit_manager.rs:124`, backs both Render Studio's reveal buttons and Demo Auditor's) just spawns `explorer /select,<path>` and only propagates an error if launching `explorer.exe` itself fails — it never checks the target path exists, and Windows' `explorer /select` doesn't error on a bad path either, it just falls back to its default view. **Fixed**: added a `path_buf.exists()` check up front, returning `Err("Path no longer exists: {path}")` before ever spawning Explorer, which flows into the toast wiring `ipc_bridge.js` already had (`Could not open folder: ...`). Re-verified via a slightly different path than originally planned: started a real render batch, clicked Open Take Folder mid-render (worked), let it finish, renamed the take folder, clicked "Reset to Queued" — job immediately went `Error` with an accurate `Take folder not found: ...` log entry (a separate, already-correct guard at the actual FFmpeg-invocation level, unrelated to this bug), then clicking "Open Take Folder" on that Error row correctly toasted `Could not open folder: Path no longer exists: ...` instead of silently opening the wrong folder.

### ⚠️ Rendered audio was broken two different ways — both found 2026-08-22, mid `MIN_TAKE_SEPARATION_SECONDS` calibration testing
Started as one symptom ("rendered audio sounds awful, like static/a helicopter") that turned out to be two independent, stacked bugs. Raw `sound.wav` in the take folder was correct throughout both — confirms the corruption was always introduced during the render/mux step, never at capture.

- [x] **Bug 1 — Render Studio's FPS has no connection to the FPS a take was actually captured at, causing sped-up video.** User captured at 120fps but Render Studio's Codec/FPS panel still had `300` (its last-used value from earlier testing, per the VirtualDub-style per-job settings model — see the 2026-08-18 "Render Studio: per-job settings" entry in Completed Tasks); render log confirmed `fps=300` on every batch that night. Root cause: `renderer.rs:31`'s `config.fps` feeds `-framerate` *before* `-i hudcolor/%05d.bmp` — that flag tells ffmpeg how to interpret the BMP sequence's timing (`computed video duration = frame_count / declared_fps`), so a wrong value produces a wrong computed duration (2.5x too fast at 300 vs. an actual 120fps capture), and `-shortest` then trims output against that wrong duration. No error surfaced; render reports success either way. **There is still no source of truth anywhere linking a take folder to its actual capture FPS** — `config.fps` is the only place `hlcr/renderer.rs` gets `fps` from, entirely independent of Capture Studio's own Capture FPS setting at capture time. User confirmed re-rendering with FPS manually matched (120) fixed the sped-up video — verified via `ffprobe`, rendered duration now matches the take's own `sound.wav` duration exactly (14.83s both). **Diagnosed and manually worked around 2026-08-22; the missing source of truth was added 2026-08-27.** `native/src/hlcr/take_meta.rs` records the batch's `mirv_movie_fps` in a `dodtools_take.json` in each take's own block folder, and `run_render_job` reads it and logs `[render-fps-mismatch]` naming both rates, the resulting speed factor and its direction, and the value to set instead.

**Per take, not per session** — the first cut wrote one file per session folder and the user caught the hole in it immediately by asking what happens when highlights are captured at one FPS and something else at another. Different batches do get different session folders (`session_YYYYMMDD_HHMMSS`, generated per dispatch), so a session-level file is correct for that case; and FPS cannot vary *within* a batch, since `capture_fps` is a single `PatcherConfig` value and `mirv_movie_fps` is set once in the init commands. What it is not correct for is a take being **moved**: a session-level file describes a location, so a take dragged into another session's folder silently inherits that session's settings — this exact bug, one level up. A per-take file travels with the take.

Writing into the block folder collides with `take_folder_has_content`, which decides a take was captured by asking whether its folder is non-empty — an empty block plus our file would report a capture that never happened. Handled by `take_meta::is_metadata`, which that check now consults, rather than by writing somewhere else and hoping nobody re-runs verification on an old session later. It is still written after verification and only for blocks that landed, but that ordering is now belt-and-braces rather than the only thing holding it up.

Deliberately **advisory, option (b) rather than (a)**: the renderer states the disagreement and renders exactly what was asked. A take folder can be moved, copied or hand-assembled, and a renderer that silently substituted a number found in a neighbouring file would be a worse surprise than the one it fixes. Takes captured before this exists have no file and produce no warning, which is the correct silence rather than a defect.

Worth recording as a near-miss: the first version sent the warning as a `RenderUpdate::Status`. `job.status` is a state field with a fixed vocabulary (`"Queued" | "Rendering" | "Finished" | "Error" | "Cancelled"`) that the scheduler's own transitions test against — `if job.status == "Rendering"` gates the Finished transition and `active_count` counts it — so a sentence in that field would have shown a paragraph where a status chip belongs and stranded any job that returned before `"Rendering"` overwrote it.
- [x] **Bug 2 — MP4 output used a PCM audio box (`ipcm`) that FFmpeg/VLC read fine but Vegas Pro can't, producing garbled/static audio independent of the FPS bug above.** Found after fixing Bug 1 didn't fix the audio — FPS-matched retest still sounded broken. Root-caused by direct comparison: the user's older working clips were all `.mov`/ProRes, never `.mp4`; every render since the 2026-08-18 "H.264 output was always `.mov`" fix (which made real `.mp4` output possible) shares the exact same `-c:a pcm_s16le` line (unchanged since `pcm_s16le` was originally ported straight from the user's own Python HLCR tool, `git bda4f7a`) — the only variable that changed was the container. Confirmed empirically, not just by inspection: remuxing the *exact same* PCM audio bytes (`ffmpeg -c copy`) into `.mov` instead of `.mp4` made ffmpeg auto-select the older, broadly-supported `sowt` tag instead of `ipcm`, and the user confirmed that `.mov` remux sounds correct in Vegas. MP4 has never had solid native support for raw PCM audio in the first place (audio-in-MP4 is conventionally AAC almost everywhere) — trying to force the `sowt` tag onto an MP4 output directly failed outright (`Tag sowt incompatible with output codec id 'ipcm'`), confirming this needs a real codec change, not a tag override. **Fixed**: `renderer.rs` now picks `-c:a aac -b:a 192k` for the MP4-outputting presets (h264_nvenc, libx264) and keeps `-c:a pcm_s16le` for the MOV-outputting ones (ProRes, DNxHD/alpha-HUD), decided in the same `file_ext` match so the two can't drift apart, mirroring the existing video-codec/extension pattern. Verified via a real render (h264_nvenc + AAC) — `ffprobe` confirms `aac (LC) (mp4a)`, standard/correct — **and the user confirmed the AAC test render plays back correctly in Vegas Pro.** Fully resolved.
- Follow-up from Bug 1 (no source of truth links a take to its capture FPS) is now tracked as [issue #14](https://github.com/ccoventry/dod-tools/issues/14).
- [x] **Run one real HLAE capture against `feature/capture-block-manifest` (2026-08-18).** Took several attempts, each surfacing a real bug fixed along the way:
  - **`hl.exe` opened then closed within a few seconds, no crash logged.** Root cause: `spawn_capture_engine` (`native/src/capture_engine.rs`) spawned HLAE and then only polled for the done-marker/exit-trigger files — it never checked whether the child process was still alive, so a real early failure would just hang the wait loop forever with nothing logged. Added `child.try_wait()` polling plus full `[HLAE]` timing logs to `crash_log.md` (spawn args, PID, launcher exit, taskkill/cancel reasons).
  - **That fix immediately false-positived**: the HLAE launcher process itself exits ~2.5s after spawn *by design* once it hands off to `hl.exe` (`-noGui -autoStart`) — that's normal, not a crash. The first version of the liveness check treated any launcher exit as fatal and ran the abort-cleanup path, which deletes the just-copied `primer.dem`/`chain_01.dem` from `dod/`. That race (cleanup deleting the demo while `hl.exe` was still starting up) is what produced a real in-game `ERROR: couldn't open` on `primer.dem` — the file itself was never corrupt (confirmed: the original patched copy was intact, byte-count matched the copy log exactly). Fixed by tracking `hl.exe` liveness via process name (`sysinfo`) instead of the launcher's own exit status, with a 5s grace period and a separate "was alive, now gone with no exit trigger" check for genuine mid-capture crashes.
  - **`capture_takes_verified` reported `captured: true, renderable: false` for every block** — the real failure mode this test was built to catch, but not for the reason suspected (`.wav` flush timing). Actual cause: HLAE's `mirv_movie` plugin auto-numbers each recording into a `take0000`, `take0001`, ... subfolder under whatever directory `mirv_movie_filename` points at, to avoid overwriting a previous take at the same path — so the wav/bmp sequence lands one level deeper (`chain_01_b0/take0000/sound.wav`) than `hlcr::scanner::is_renderable_take` (`native/src/hlcr/scanner.rs`) checked. Render Studio's real folder scanner (`scan_folder_background`) was never affected since it recurses via `WalkDir`; only this newer single-folder check was wrong. Fixed by checking one level into any `take*` subfolder too, with a unit test reproducing the exact on-disk layout from the failing run.
  - **Also fixed while debugging**: `cancel_capture_batch` and the capture event-listener thread now log to `crash_log.md` too (`desktop-studio/src-tauri/src/capture_manager.rs`), and the listener thread is wrapped in `catch_unwind` so a panic there logs and resets `is_running` instead of leaving Start Capture Batch permanently greyed out.
  - **Final clean run (21:53, 2 blocks):** `captured=true renderable=true` for both. Phase 3 unblocked.
  - Not exercised this pass: `mirv_movie_separate_hud 0` writing BMPs directly into the take folder (both test runs used the default `all`/`hudcolor`-subfolder layout) — revisit if that setting is ever turned on for a real capture.
- [x] **RESOLVED — `wsod25-po_r3_sf-m00cat_ih_m3_armory_h2.dem` crash was a corrupted source file, not a bug anywhere in dod-tools (2026-08-22/23).** This was the calibration test candidate for the item below (closest natural highlight gap across all 429 Steam demos, rows #8/#9). Two real capture attempts through the app both ended in `hl.exe` dying ~5-8s after HLAE handed off (`capture_engine.rs`'s "was running and is now gone with no exit trigger" detection, working correctly) — visually confirmed by the user as the DoD menu backdrop showing briefly, then a black screen, then the process closing a few seconds later. Root-cause chain: (1) `qconsole.log` never gets created even with Add Condebug on — `hl.exe` dies before finishing engine init; (2) the map (`dod_armory_b6`, confirmed via `dod-tools-inspect`'s embedded map-name read) *is* installed, ruling out a missing-map crash; (3) manually copying the same source demo into `dod/` and running `playdemo` directly through a normal Steam-launched session (bypassing the app) reproduced the identical crash — ruling out the patcher/capture-orchestration entirely; (4) **actual root cause, found by comparing against a second copy of the same demo the user had**: the file used all night (`wsod25-po_r3_sf-m00cat_ih_m3_armory_h2.dem`, no suffix) is a thumb-drive-transferred copy missing ~62% of its frames (236,586 vs. 631,902 in a known-good Discord-uploaded copy, `..._h2-1.dem`, confirmed via `dod-tools-inspect`'s frame counts) despite being only marginally smaller in bytes — consistent with a bad/interrupted USB transfer silently dropping large chunks of data. Well-formed enough for this repo's own `nom`-based parser to read without erroring (unlike the outright-corrupt `monday-wsod25_r07_*` family below, which fails at the parse level) but not well-formed enough for GoldSrc to actually play back. **Not a dod-tools bug** — no code changes from this investigation. The user has the good `..._h2-1.dem` copy to resume calibration testing with if they want to keep using this exact demo/rows rather than switching to the fallback candidate below.
- Calibrating `MIN_TAKE_SEPARATION_SECONDS` against real HLAE finalization time is now tracked as [issue #9](https://github.com/ccoventry/dod-tools/issues/9). (This candidate-hunting pass is also what surfaced the crash-fix/corruption items below — that context stays here since it's not itself an open task.)
- [x] **Fixed a real crash in the demo parser, found while hunting for merge/calibration candidates (2026-08-22).** `dem-patch/src/netmsg_doer/delta_description.rs`'s `SvcDeltaDescription::parse` unconditionally `.unwrap()`ed the `name`/`bits`/`divisor`/`flags` fields out of each parsed `delta_description_t` entry. Some demos delta-compress consecutive entries within the same message against each other (fields not marked "changed" in that entry's bitmask are simply absent, not zero), which this parser doesn't track a baseline for — hitting that produced `called Option::unwrap() on a None value` followed by a full process crash (`STATUS_STACK_BUFFER_OVERRUN`, not just a normal panic exit), reproduced in isolation against `monday-wsod25_r07_m3_h1-1.dem`. This wasn't confined to the `find_overlaps` CLI: `capture_manager.rs`'s real folder scan (`scan_directory_impl`, `desktop-studio/src-tauri/src/capture_manager.rs:735-746`) calls the same `scan_demo_for_highlights_with_analysis` with no `catch_unwind`, only `if let Ok(...)` — a panic there isn't caught either, so this same demo dropped into Capture Studio's "Add Demo Files" would very likely have crashed the whole app. Fixed by converting the unwraps into a proper `nom_fail(...)` parse error (same pattern already used at every other decoder-lookup failure in this file's siblings — `client_data.rs`, `event.rs`, etc.), so a demo that hits this now lands in the existing "skipped (HLTV/unreadable)" bucket instead of taking down the process. Deliberately did **not** attempt to actually decode these entries correctly (i.e., implement real baseline carry-forward) — see the item directly below for why that needs more certainty first. Verified: isolated repro no longer crashes (clean skip with a real error message); `cargo test --workspace --no-fail-fast` shows the same 4 pre-existing `analysis` failures as every prior pass, nothing new.
- Investigating/repairing `monday-wsod25_r07_m3_h1-1.dem` and 3 sibling corrupted demos is now tracked as [issue #15](https://github.com/ccoventry/dod-tools/issues/15).
- [x] **Phase 3 — take index + auto-Rendered, manually verified end-to-end (2026-08-18/19), including two more real bugs found on retest.** Real HLAE capture → save → full app restart → reload → render → confirmed `Rendered` status resolved correctly from the persisted `takeIndex`, proven via explicit `[take-index]` console logs at both the load point and the resolve point (not inferred — the log at resolve time reports the same entry count the load-time log reported, and neither `capture_pane.js`'s in-memory `lastDispatch` nor any other code path can populate `takeIndex` except Load Session, so a match is only possible if it came from the reloaded file). One real correlation bug found and fixed along the way: `take_key()` (`native/src/shared/paths.rs`) assumed its input was always the block folder directly, but Render Studio's real scanner finds takes one level deeper inside HLAE's auto-numbered `take0000` subfolder (the same nesting quirk that broke `is_renderable_take` above) — without the fix, capture-side and render-side keys would never have matched and auto-Rendered would have silently never fired. Fixed with two tests reproducing the exact mismatch.
  - **First retest attempt: `render_take_finished` never fired at all**, despite both jobs finishing correctly in the UI. Cause: the emission was gated on `job.status == "Rendering"` inside `apply_render_update`'s `Finished` handler, but on the real success path `run_render_job` sends `Status("Finished")` *before* `Finished(true, None)` — so by the time `Finished` is processed, `job.status` already reads `"Finished"` and that guard always fails. The guard's actual job is only to stop this generic transition from clobbering a more specific status a preceding `Status` update already set (e.g. never overwrite `"Cancelled"` with `"Error"`) — a pre-existing ordering quirk, just newly exposed by putting new logic inside it. Fixed by keying the capture on `success` alone, independent of that guard.
  - **Second retest attempt found the H.264/NVENC output was always `.mov` despite the Codec dropdown labeling both "MP4".** `run_render_job` hardcoded `.mov` for every codec; the correct mapping existed (`CodecPreset` in `config.rs`) but had zero call sites anywhere — dead, and separately wrong on its own terms (string-matched `"h264"` for `.mp4`, but `RenderCodec::NvencH264` maps to the distinct `"h264_nvenc"` id, left over from before software/NVENC were split into two variants). Fixed by deciding the extension in the same match that picks the codec args; removed the dead/stale function rather than leave a second wrong source of truth around. Verified the container change was actually safe (not just cosmetic) by running this repo's own `tools/ffmpeg.exe` to mux the hardcoded `pcm_s16le` audio into a real `.mp4` before shipping the fix — succeeded and read back cleanly.
  - **Final retest, with screenshots: fully confirmed.** Toasts read exactly as expected ("1 highlight(s) marked Rendered." ×2, "Render batch completed successfully!"), Highlight Details/Master Queue show `Rendered` status on both highlights (including the merged-block row from Phase 1's overlap merge, confirming Phases 1–3 remain coherent together), and the rendered files show as genuine MP4 in Explorer/VLC.
  - Also cleaned out ~90GB of accumulated raw capture test folders from prior debugging sessions (`C:\...\02_HLAE_Raw`) before this retest — unrelated to the code, just session hygiene.
- [x] **Render Studio: fixed a Start/Cancel button-state desync + added missing persistent logging (2026-08-18).** Found while testing Phase 3: `reset_render_job` can resume the render scheduler on its own (by design — see the per-job settings item below), but `render_pane.js`'s Start/Cancel buttons were only ever updated inside the Start button's own click handler and `render_batch_finished`, neither of which fire on a reset-triggered resume. Result: "Start Render Batch" stayed clickable while a batch was genuinely running, repeatedly failing with "Render batch already in progress" — confusing, but not data-destructive. Fixed by driving button state off the live `render_jobs_snapshot` payload instead. Also: `render_manager.rs` had zero persistent logging anywhere (confirmed — a full cancel/reset/re-render cycle produced no `crash_log.md` entries at all); added `log_markdown` calls at every render-side state transition (batch start/reject, no-takes-found, cancel batch, cancel job, reset job + whether it resumed, scheduler exit, and each `render_take_finished`).
- [x] **Render Studio: per-job settings, VirtualDub-style (2026-08-18).** Raised by the button-state bug above: `reset_render_job` re-armed the scheduler using `last_config`, a single `RenderConfig` snapshot captured once at the original "Start Render Batch" click — so changing the Codec/FPS panel after cancelling and clicking Reset to Queued would have silently re-rendered with the *old* settings. Modeled after VirtualDub's Job Control instead of patching that one bug: each render job now snapshots its own `codec`/`fps` at scan time (`RenderJobRuntime`, `render_manager.rs`); the Codec/FPS/Concurrency panel is a template for *new* jobs only, safe to edit any time; `reset_render_job` never touches a job's own settings, so a reset always re-renders with exactly what that job was queued with — predictable, no more silent staleness. New **Settings** column in the render jobs table (`"ProRes @ 300fps"`) shows each row's actual settings as one summary string rather than one column per setting, so a future new setting doesn't need its own column. `.render_autosave.json` now persists the codec as a stable string id (`to_str_id`/`from_str_id`, round-trip tested) instead of Rust `Debug` formatting. **Known, pre-existing, un-widened limitation**: crash recovery (`recover_render_batch`) only ever had one session-wide codec/fps to recover with (autosave never recorded per-job settings) — a job individually reset to a different codec before a crash won't recover with that override. **Deliberately deferred**: bulk-editing settings on already-queued/cancelled jobs without a full rescan needs multi-select, which doesn't exist anywhere in Render Studio yet (same gap already tracked for Capture Studio's Clear Selected in Phase 4 of the connected-workspace plan) — for now, changing settings on existing takes means Cancel All → change panel → Start Render Batch again (fresh scan, always picks up current settings).
- [x] **Phase 4 — Workspace vs Quick-Clip modes, implemented (2026-08-19) and extensively manually verified since (2026-08-19/20 — see the several follow-up entries below this one for the real bugs that verification found).** Plan: `~/.claude/plans/splendid-shimmying-kahn.md`. Adds `AppSettings.studio_mode` (`"quick-clip"` default | `"workspace"`, `settings_manager.rs`) with a toggle in the top nav — a labeled slider switch (`.studio-mode-switch`), not two buttons, and deliberately not sharing the `.nav-tab-btn` class: an earlier version reused that class for two mode buttons and broke the app on click, because `main.js`'s generic `.nav-tab-btn` click handler (`main.js:822`) treats every element with that class as a top-level nav destination and calls `switchNavTab()` with its `data-nav` attribute — the mode buttons had none, so `switchNavTab(undefined)` matched no routing branch and just hid every pane. Found by the user immediately on first manual click-through (2026-08-19); fixed by rebuilding the control as a checkbox-driven slider with its own `.studio-mode-label`/`.studio-mode-switch` classes that the nav router never touches. Each label carries a tooltip, plus a persistent caption line next to the toggle so the active mode's meaning is visible without hovering. Master Demo Queue gained three buttons — **Clear Untracked** (Workspace: removes only demos with no tracked work and toasts what it kept; Quick-Clip: wipes everything behind one `confirm()`, nothing is protected there), **Clear Selected** (new — first checkbox/multi-select mechanism `master_pane.js` has ever had, tracked by demo path in a module-level `Set` so a mid-list delete can't desync it from array indices), and **Clear All** (Quick-Clip: same blunt wipe as Clear Untracked; Workspace: a modal enumerating how many demos have tracked work plus a "Save Session First" option that reuses the same `saveProjectSession()` the Save button calls). "Tracked" (`take_index.js`'s new `isHighlightTracked`/`isDemoTracked`) is deliberately a wide net per user direction (2026-08-19): status moved off `None`, OR a note, OR a narrowed kill range — reusing the kill-range check that already existed in `detail_pane.js` rather than re-deriving it. Re-scan behavior is now mode-gated too: Workspace keeps Phase 2's merge-by-uid (`preserveHighlightState`); Quick-Clip deliberately reverts to replacing the demo wholesale, since nothing is meant to survive there anyway. Project JSON gains an informational `mode` field and bumps to `0.12.0`; Load Session always forces Workspace regardless of that field's value. Saving from Quick-Clip mode ("Save as Workspace…", the button's label in that mode) both writes the file and switches the window to Workspace. Also added the plan's **merged-block affordance**: a highlight whose take covered more than one source highlight (an overlap merge) now shows a small "merged → chain_NN_bN" badge in Highlight Details (`capture_pane.js`'s `capture_takes_verified` handler tags the covered streaks non-enumerably, same pattern as `.uid`, so it can't leak into a future capture payload). Pre-emptively deleted `capture_pane.js`'s `updateRowBadges` — confirmed dead (blindly rewrote every `td span` in the Master Queue body, which has rendered zero spans since the 2026-08-16 queue redesign dropped the per-row status badge) and would have started corrupting the new checkbox column's cells the moment it grew any `<span>`. Naming: "Clear Discovered" from the original backlog item was renamed to **"Clear Untracked"** per user feedback (2026-08-19) — "Discovered" described how a demo entered the queue, not what the button does. Two more refinements from that same manual click-through: **Clear Selected is now disabled until at least one row is checked** (`master_pane.js` syncs the button's `disabled` state on every row/select-all checkbox change and re-render, not just once at load); and **Clear Selected now escalates to the same tracked-work warning modal Clear All uses** whenever any checked demo is tracked, in either mode — the modal (`#clear-all-modal`, now genuinely shared rather than Clear-All-only) took a `pendingClearTargets` parameter so its Confirm/Save-First buttons operate on whatever set (all demos, or just the checked ones) opened it; an untracked-only selection still just gets the quick native `confirm()`. **Verification:** `cargo build --workspace` and `cargo test --workspace --no-fail-fast` both clean (same 4 pre-existing `analysis` failures as every prior pass, nothing new; added two `settings_manager.rs` assertions covering `studio_mode`'s default and round-trip). `npm run build` clean. **Update (2026-08-20):** the plan's own verification step — exercising both modes' three Clear actions against a real running app — is now done; see the crash fixes, the `confirm()` fix, and the selection-preservation fixes below, all found by exactly that manual click-through. Still not exercised: the "Merged Blocks" (needs a real capture batch) and "Settings Persistence" (needs an app restart) sections of `docs/qa_checklists/phase4-connected-workspace.json`.
- [x] **Phase 4 manual verification pass (2026-08-19), several real bugs found and fixed.** User ran the app and worked through it live; every finding below was a real click-through result, not code review. (1) The mode-toggle/nav-router collision described above. (2) Toggling modes shifted the whole nav bar because `#save-project-btn`'s two labels ("Save Session" vs "Save as Workspace…") were different widths under `#top-navbar`'s `space-between` layout — fixed with a fixed `width` + `text-overflow: ellipsis` on the button. (3) The Master Queue's checkbox column header drifted from its `<td>`s at wide window sizes — a `<th>` inherits the table's left `text-align` by default while the `<td>`s were centered inline; same root cause hit every other centered column (Highlights/Pending/Captured/Rendered/Actions), fixed with a shared `.col-center` class on all six `<th>`s. (4) Added a tracked-work badge (bookmark icon) to Master Queue rows — then found Kill Range and Notes edits in `detail_pane.js` never fired any callback back out to `main.js`/`master_pane.js` at all (only the Status dropdown did), so the badge (and the whole Master Queue) went stale after either edit; fixed by wiring `currentOnSelectionChange()` into Kill Range's start/end/reset handlers and Notes' `change` event (deliberately not `input`, so typing a note doesn't rebuild the table every keystroke). (5) `Pending` originally counted as a "tracked" status alongside `Captured`/`Rendered`, but since the dropdown displays an untouched `undefined` status as "Pending" too (pure display convenience, `detail_pane.js`), re-selecting the same-looking value could silently flip a row's tracked state while every other untouched row still showed "Pending" with no badge — narrowed `isHighlightTracked` (`take_index.js`) to only `Captured`/`Rendered`, and reworded every tracked-related tooltip/confirm string to say "Captured/Rendered status" instead of "status" so this can't recur silently. (6) **Clear All and Clear Untracked only checked for tracked work in Workspace mode**, while Clear Selected and the row-delete button (added the same day) checked in *both* modes — so in Quick-Clip, deleting one tracked row warned you but Clear All didn't, an inconsistency the user caught by comparing the two. Resolved by dropping the `studioMode` branch from both entirely: Clear Untracked now always removes only untracked demos (its own name already implies that, no mode-gating needed), and Clear All always escalates to the shared modal when something's tracked, in either mode — Quick-Clip now only differs from Workspace in *persistence* (nothing auto-saves, re-scan replaces wholesale), never in whether tracked work is protected from the Clear actions. Every mode-toggle tooltip/caption string was reworded to match (no longer claims Quick-Clip's Clear Untracked "wipes the whole queue instantly").
- [x] **QA checklist tooling built for this verification pass (2026-08-19), not itself part of the feature.** `docs/qa_checklists/` — a zero-dependency Node static server (`server.js`) plus `manifest.html`, a clickable-checkbox test-checklist page styled to match the app. One `.json` file per checklist (schema in that folder's `README.md`); the server does a real directory listing on every request, so dropping a new checklist file in and refreshing the page is genuinely live, no rebuild step. (A version was also published as a Claude artifact first, but that one needed manual regeneration to pick up new checklists since a sandboxed browser page can't read the local repo — dropped in favor of the local server per user preference, 2026-08-19.) `docs/archive/phase4_manual_test_checklist.md` is a plain-Markdown mirror of the same content, kept in sync by hand, for anyone who'd rather read it in an editor than a browser.
- [x] **Clear actions scoped to the active search filter (2026-08-19), found mid-testing.** User was verifying the header select-all checkbox's "only affects visible rows" behavior and noticed Clear Untracked/Selected/All had no equivalent scoping at all — they acted on the *entire* queue regardless of an active search, while the select-all checkbox and (separately) Clear Selected's checked-row targeting already respected it. Fixed by extracting the search-match logic in `master_pane.js` into one `matchesSearch()`/`getVisibleDemos()` (exported) that rendering, the select-all checkbox, and every Clear handler in `main.js` now all read from, so they can't drift apart on what "visible" means again. Clear Untracked and Clear All now only ever act on demos matching the current search (a demo hidden by the search box survives regardless of tracked status); Clear Selected now also excludes checked-but-hidden rows, leaving them in the queue untouched. Per user request, every affected confirm()/modal summary/success toast gets an explicit callout — `` (search filter active — only considered N of M demo(s) in the queue)`` — whenever the filter actually narrowed what was acted on, so none of the three silently does less than its label implies; the callout is empty (omitted) when the search box is empty. `requestTrackedClearConfirmation()` gained an optional `filterNote` parameter for this. New coverage added to both `docs/qa_checklists/phase4-connected-workspace.json` and its Markdown mirror.
- [x] **Two more real bugs found immediately after, same session (2026-08-19).** (1) Clear Untracked's "kept N with tracked work" toast math was `totalCount - removedCount`, which silently assumed everything not removed must be tracked — true before the search-scoping fix above, false after: a demo left alone because it's simply hidden by the search (never evaluated at all) got mislabeled as "kept ... with tracked work." Caught by the user with a concrete repro (2 untracked demos, search narrows to 1 visible, clear it — toast wrongly claimed 1 was kept for tracked work when the hidden one was untracked too). Fixed by computing `trackedVisibleCount` from the visible set specifically and only appending the "kept" clause when that's nonzero. (2) The shared Clear modal's Confirm button had its label hardcoded in `index.html` as "Clear All Anyway" — accurate for Clear All, wrong for Clear Selected (still said "Clear All Anyway" despite the modal's own title correctly reading "Clear Selected Demos") and for the row-delete case. Added a `confirmLabel` option to `requestTrackedClearConfirmation()`, same pattern as the existing `title` option, so each of the three callers sets its own: "Clear Selected Anyway", "Clear All Anyway", "Remove Anyway" (row delete). Both fixes verified with rebuild; checklist coverage added for the exact repro and for each caller's modal title/button-label pairing.
- [x] **Master Demo Queue's Clear actions now log to `crash_log.md` (2026-08-19), found mid-testing.** User noticed the Clear Untracked/Selected/All buttons and row delete — all four pure frontend array mutations, with no other Tauri call to piggyback on — left zero trace in `crash_log.md`, unlike capture/render actions which log liberally. Added a new `log_frontend_event(message)` Tauri command (`lib.rs`) that's just a thin passthrough to `native::log_markdown`, plus an `ipc_bridge.js` wrapper (`logFrontendEvent`, best-effort — console-logs on failure rather than toasting, since a lost log line isn't worth interrupting the user over). Each of the four actions logs one `[queue] ...` line on completion — action name, count removed, filter-scope/save-first context, and the removed demos' names — after `master_pane.js` picked up a new `import { logFrontendEvent } from './ipc_bridge.js'` (no circular-import risk checked first, since `ipc_bridge.js` has zero dependency back on pane modules). `cargo build`/`cargo test` clean on the new command.
- [x] **Clear Selected no longer wipes checkboxes on rows it never touched (2026-08-19), raised as a question mid-testing.** User asked whether Clear Selected clearing *every* checked row's checkbox — including ones hidden by the search filter and therefore left in the queue untouched — was intentional. It wasn't a considered choice, just an artifact of `replaceScannedDemos()` always calling `clearCheckedPaths()` unconditionally; agreed it was inconsistent with the whole point of the earlier search-scoping fix (leave what's hidden alone) to then silently deselect it anyway. Added `setCheckedDemoPaths()` to `master_pane.js` (replaces the checked Set outright, re-syncing the Clear Selected button's disabled state) so `main.js`'s handler can snapshot which checked paths are hidden-and-not-being-removed *before* `replaceScannedDemos()` clears everything, then restore just those afterward. Also fixed inconsistent "checked" vs "selected" wording across the three related toast/confirm strings while in the area (the button and its own confirm() already said "selected"; two toasts still said "checked") and added the missing count to "All N selected demo(s) are hidden by the current search" (previously said "All checked demos" with no number).
- [x] **Critical: Clear All could crash the app with a stack overflow (2026-08-19), found via user report of a missing toast.** User reported Clear All silently produced no toast and stopped writing to `crash_log.md` mid-session — initially investigated as a possible logic bug (ruled out: the code path is identical to the working Save-First path, which does show its toast), then as a possible stale dev-server build (ruled out by asking for a fresh reload), and only found once the user checked devtools and shared the actual `RangeError: Maximum call stack size exceeded` with a full stack trace. Real bug: `master_pane.js`'s `renderMasterList()` notifies `currentOnSelectDemo(null, null)` every time it renders an empty queue; after Load Session, that callback calls `renderDetailView(null, null)`, which cascades through `updatePreviewButtonStates` into `detail_pane.js`'s `onSelectionChange` callback (`main.js:1115`), which calls `renderMasterList()` again — still empty, notifies again, infinitely. Only reachable with a loaded session (to register that specific callback) *and* an action that empties the queue to exactly zero; Clear All emptying the whole queue was the first thing in this pass able to trigger both preconditions. Fixed with a `wasEmptyQueue` flag so the notification only fires on the transition into empty, not on every re-render of an already-empty queue. Full writeup in `bugs.md`. Worth noting for future debugging: a silently-stopped crash mid-async-chain looks identical to "the code just doesn't run" from the outside (no toast, no log line) — the devtools console was what actually distinguished it from a logic bug.
- [x] **Second crash in the same area, found immediately by re-testing without a loaded session (2026-08-19).** The recursion fix above only guarded the notification itself — it didn't audit every `onSelectDemo` callback the notification can reach. User re-ran the same Clear-All-to-empty regression test but via a plain "+ Add Demo Files" scan instead of Load Session, hitting a *different* registered callback (`main.js:655`) that unconditionally read `demo.path` with no null check — `renderMasterList`'s `(null, null)` empty-queue notification had simply never reached it before. `TypeError: Cannot read properties of null (reading 'path')`. Fixed with an explicit null guard; also audited `renderDetailView`/`updatePreviewButtonStates` (already safe) rather than assuming this was the only gap. Lesson for this whole area: any code path reachable from `renderMasterList`'s empty-queue notification needs to tolerate a null demo, not just the one path a given regression test happens to exercise.
- [x] **Global error reporter added (2026-08-19), directly motivated by the two crashes above producing zero visible symptoms without devtools open.** New `error_reporter.js`, registered at module load in `main.js` (before `DOMContentLoaded`, catching from the earliest possible moment) — `window.addEventListener('error', ...)` and `('unhandledrejection', ...)`. Not a try/catch anywhere; a pure safety net for whatever nothing else already handled, so it can't interfere with or duplicate any existing `.catch()`-based error toast. On any truly uncaught error: a tagged `console.error` (alongside the browser's own default uncaught-error logging, not replacing it — user specifically wanted multiple ways to notice, having a devtools console open in one screen quadrant much of the time), a `[error] ...` line in `crash_log.md` via the existing `logFrontendEvent` bridge, and a toast — capped at 3 per session so a rapid-fire repeating bug (like the recursion loop above) can't spam the screen, though every occurrence still gets logged/console'd regardless of the toast cap. New `docs/qa_checklists/error-reporter.json` checklist for it.
- [x] **Critical, wider-reaching: every `confirm()` dialog in the app never actually blocked on the user's answer (2026-08-19), found immediately after the crash fix above.** After fixing the stack overflow, Clear All's plain-`confirm()` path (nothing tracked, no modal) still threw `dialog.confirm not allowed. Command not found` in devtools — and the demos still got removed regardless. Investigated with WebSearch/WebFetch since this needed authoritative info on Tauri's actual behavior, not guessing: Tauri v2 intercepts the global `window.confirm`/`window.alert` to route through the dialog plugin, but that interception (a) needs the `dialog:allow-confirm`/`allow-ask` permissions specifically (not bundled in the already-granted `dialog:default`, confirmed via the generated `desktop-schema.json`), and (b) is implemented as an `async` override — while every call site in this codebase used it synchronously (`if (!confirm(...))`). Since a pending Promise is truthy, `!confirm(...)` was *always* `false`, meaning every confirmation in the whole app — including two pre-existing ones from before this session, Demo Auditor's duplicate-file delete and Capture Studio's orphaned-preview delete — always proceeded as if the user clicked OK, dialog or no dialog. The permission gap just made the failure visible as a console error; without it, the bug would've been fully silent. Fixed by abandoning the global `window.confirm` entirely and switching all five call sites (`main.js` ×2, `master_pane.js`, `auditor_pane.js`, `capture_pane.js`) to the explicit, properly-`Promise<boolean>` `confirm()` exported by `@tauri-apps/plugin-dialog`, awaited everywhere it's called — confirmed via the plugin's own `.d.ts`/source that this routes through the `message` command, already covered by `dialog:default`, so no capability file changes were needed after all (a `dialog:allow-confirm`/`allow-ask` addition was tried first, then reverted once the real fix — stop using the global override — made it unnecessary). Full writeup in `bugs.md`.
- [x] **Three selection-preservation bugs, all one root cause each, found via a detailed test report (2026-08-20).** User systematically tested Row Delete/Clear Untracked/Clear Selected against "does the right demo stay selected/highlighted afterward" and found: (1) `replaceScannedDemos()` (shared by all three Clear actions) always hard-reset `selectedDemoIdx` to row 0 after clearing, never checking whether the previously-selected demo was still in the queue — so Clear Untracked/Selected jumping to row 1 even when the selected demo survived the clear untouched. Fixed by capturing the selected demo *by object identity* before the swap and looking it up in the new array afterward (`currentScannedDemos.indexOf(previouslySelectedDemo)`), only falling back to row 0 if that demo is actually gone — also fixed a latent bug in the same function where `renderDetailView` was called with `currentScannedDemos[0]` instead of `currentScannedDemos[selectedDemoIdx]` (harmless before since `selectedDemoIdx` was always 0, not harmless now). (2) Separately, `master_pane.js`'s row-delete handler always called `renderMasterList(currentDemos, null, ...)` — hardcoding *no* selection on every delete, regardless of what `main.js`'s `onDeleteDemo` had actually decided (which was already correct, hence Highlight Details staying right — only the row highlight itself was dropped). Fixed by having `onDeleteDemo` return the surviving `selectedDemoIdx` and passing that through instead of `null`. Both fixes preserve correctly under an active search filter too, since identity lookup runs against the full `currentScannedDemos` array, not the filtered view. Checklist coverage added to all four affected sections (`docs/qa_checklists/phase4-connected-workspace.json`). Also logged, not fixed: Highlight Details' Select All/Deselect All buttons don't disable when nothing's selected (Low Priority backlog, purely cosmetic per user).

### Capture Studio Feature Parity & UI Restorations
- [x] **Task: Verify Branch Merge Readiness (2026-08-16):** `dev`'s tip (`80feaaf`) *is* the merge-base with `feature/tauri-migration` — the branch already contains 100% of `dev`'s history, nothing to reconcile. `cargo build --workspace` clean; `cargo test --workspace --no-fail-fast` clean except 4 failures in `analysis`, confirmed byte-identical/pre-existing at the `dev` merge-base (not caused by this branch) — see Medium Priority below.
- [x] **Task: Full Re-Verification After 2026-08-16/17 Fixes + xash-transcode Split (2026-08-18):** Re-ran full `cargo build --workspace` (clean) and `cargo test --workspace --no-fail-fast` after the batch-capture/session/POV-filter fixes and the `xash-transcode` branch split — same 4 pre-existing `analysis` failures as above, no new failures. `feature/tauri-migration` has no remaining merge blockers.
- [x] **Task: Fix pre-existing `analysis` test debt (found already resolved 2026-08-23, doc drift — never recorded when it actually happened).** Verified via `cargo test --workspace --no-fail-fast` at `dev`'s tip (`c131ec0`, post-PR-#5 merge): 0 failures across every crate, better than the "4 pre-existing failures" baseline this doc had tracked since 2026-08-13. Checked the actual source to confirm, not just the pass count: the 3 localization tests (`test_parse_localization_content` etc., `analysis/src/localization.rs`) now correctly assert canonicalized (lowercased, `#`-stripped) key storage, matching real `parse_localization_content` behavior — reconciled at some point across the `chore/capture-allocation-cleanup`/`feature/capture-block-manifest`/`feature/capture-render-quick-wins` stack, but never logged in this doc when it happened. Per user recollection: the root cause was a real inconsistency between the game's own `dod_english.txt` (keys with no `#` prefix) and this repo's own `dodtools_english.txt` (keys *with* a `#` prefix) — two localization sources using different key conventions for the same lookup table, which is exactly why canonicalizing on insert (rather than trusting either file's raw key format) was the right fix. `test_inspect_lenn_demo` (`analysis/src/lib.rs`) isn't actually "fixed" in the sense of getting a fixture — it uses a runtime check that `eprintln!`s a skip message and returns early if the demo file isn't present, rather than failing, so it passes regardless. Not investigating further which commit did the localization fix — not load-bearing, the doc gap is what mattered here.
- [x] **`utils::test::bit_slice_to_string` — real bug, fixed (found 2026-08-22).** The test's expected string was `"*38\0\0\0\0\0"` (8 chars) but the `bitarr!` input literal is 32 bits = 4 bytes, so `bitslice_to_string` can only ever return 4 chars (`"*38\0"`, confirmed via the actual assertion diff). `bitslice_to_string` itself was correct; only the test's expected literal was wrong. Fixed in `dem-patch/src/utils.rs:87`. (Found alongside this: `dem-patch`'s 4 other ignored tests need a small tracked fixture demo — tracked as [issue #16](https://github.com/ccoventry/dod-tools/issues/16).)

---

## 🔍 Capture Studio Parity Backlog (Audited Gaps)

### High Priority
*(none currently open — all three audited High Priority parity gaps resolved 2026-08-13, see Completed Tasks)*

### Medium Priority
- [x] **Remove Chronological Allocation Strategy (2026-08-18):** Deleted entirely rather than just band-aiding it off. Removed the `DriveAllocationStrategy` enum and `PatcherConfig.allocation_strategy` field (`native/src/patch/types.rs`), its export from `patch/mod.rs`, the string↔enum mapping in `capture_manager.rs`, the `allocation_strategy` field + default fn on `AppSettings` (`settings_manager.rs`), the dropdown + footer readout in `index.html`/`styles.css`, and all read/hydrate/payload wiring in `main.js`/`capture_pane.js`. `build_batch_queue` now always uses the (now sole) Maximize Space allocator.
- [x] **Smarter Maximize Space Bin-Packing (2026-08-18):** Implemented First-Fit-Decreasing — extracted a pure `allocate_blocks_first_fit_decreasing()` helper (`native/src/patch/builder.rs`) that sorts each demo's clip blocks by byte-size estimate descending before allocating, so a large clip can't get stranded because an earlier, smaller clip already claimed the only drive with room for it (route aliases still key off each block's original index, not allocation order, so nothing downstream — scheduled commands, `mirv_movie_filename` targets — changed). Extracting it into a standalone function also made it unit-testable without real disk I/O: added `test_ffd_allocation_succeeds_where_arrival_order_would_strand_a_block` (constructs a 2-drive scenario where naive arrival-order first-fit fails but FFD succeeds) and `test_ffd_allocation_fails_when_no_drive_has_room` in `builder.rs`.

*(The remaining Medium Priority parity gaps — the demo-splitting fallback, movie_config binding, session bookkeeping/manifest logging, autosave lockfile recovery, demo path resolution, session schema/hash verification, ingestion diagnostics, global select/deselect scope, and the app-wide inline-validation pass — are now tracked as GitHub Issues [#17](https://github.com/ccoventry/dod-tools/issues/17)–[#25](https://github.com/ccoventry/dod-tools/issues/25). "Clear Discovered" was dropped from migration — it shipped 2026-08-19 as "Clear Untracked," see Completed Tasks.)*

### Medium Priority (continued)
- [x] **`crash_log.md` renamed to a daily-rotating `activity_*.md`, plus a "View Logs" nav button (2026-08-22).** Raised mid-session: the file had grown well beyond "crashes" (capture/render/frontend/queue events all log to it) and was one single ever-growing file (398KB+) with no discoverable way to find it short of knowing to go digging in AppData. Three changes, same session:
  - **"View Logs" button** in the top nav (`index.html`, next to the logo — visible on every tab, unlike Save/Load Session) opens Explorer with the log pre-selected via `@tauri-apps/plugin-opener`'s `revealItemInDir` (added as a dependency; `opener:default`, already granted, covers it — confirmed via the generated schema, no capability file changes needed). New `get_activity_log_path` Tauri command → `native::activity_log_path()`.
  - **Renamed** `crash_log.md` → `activity_YYYYMMDD.md` and centralized the path logic into `native::activity_log_path()`/`activity_log_dir()` (`native/src/lib.rs`) — also deleted a second, independent copy of the same path/write logic that had been living in `capture_engine.rs`'s `log_crash_abort!` macro (it now just calls `log_markdown`). Left the unrelated `<exe_dir>/local/crash_log.md` in `patch/engine.rs` alone — a different, narrower emergency log for the (essentially never-hit) Cbuf-overflow panic path, not part of this general activity log.
  - **Rotation: one file per calendar day** (not per app-launch/session — considered, rejected: today's heavy restart-driven testing would've produced many tiny files). The date is recomputed fresh on every write rather than cached, so a session running past midnight rolls onto the next day's file on its own without needing a restart — and now also leaves a cross-reference note in *both* files at the exact rollover point (`(session continues past midnight — see activity_YYYYMMDD.md)` / `(continued from activity_YYYYMMDD.md — same session, crossed midnight)`) so it doesn't look like the session just stopped mid-file. Old files beyond `MAX_RETAINED_ACTIVITY_LOG_DAYS` (30) are pruned automatically the first time a given launch logs anything.
  - **Not done**: the pre-existing `crash_log.md` (398KB, includes real history from tonight's HLAE crash investigation) is orphaned but left on disk — deliberately not deleted, since it's the user's history to archive/clear on their own terms.

- [x] **Capture abort message now reflects which failure it actually was (2026-08-22), found while reading a real abort log.** `capture_engine.rs`'s two `hl.exe` liveness-failure branches (never came up at all vs. was running and then vanished with no exit trigger — a genuine mid-capture crash) each logged an accurate, distinct `[HLAE]` line, but both fed the *same* generic user-facing abort message ("hl.exe never started (or crashed immediately)") regardless of which one actually happened — so a real mid-capture crash (hl.exe alive for ~9s, then gone) displayed as if it had never started at all. `failure_reason: Option<&'static str>` now carries the specific reason through to the final `log_crash_abort!` call instead of a bare bool.
- The "Details" column's `(+0:00)` gap bug is now tracked as [issue #26](https://github.com/ccoventry/dod-tools/issues/26).

### Low Priority
*(All Low Priority parity gaps are now tracked as GitHub Issues: [#10](https://github.com/ccoventry/dod-tools/issues/10) Select All/Deselect All staying enabled, [#27](https://github.com/ccoventry/dod-tools/issues/27) execution timeline, [#28](https://github.com/ccoventry/dod-tools/issues/28) keyboard shortcuts, [#29](https://github.com/ccoventry/dod-tools/issues/29) fast-forward speed parity, [#30](https://github.com/ccoventry/dod-tools/issues/30) stripping pre-existing svc_director commands.)*

---

## 🛠 General Backlog & Future Upgrades

All open items in this section (testing infrastructure, string centralization follow-ups, live capture progress, codec options, Render Studio/HLCR parity, and the R&D/architectural ideas) are now tracked as GitHub Issues [#31](https://github.com/ccoventry/dod-tools/issues/31)–[#54](https://github.com/ccoventry/dod-tools/issues/54).

`xash-transcode` (the web preview viewer proof-of-concept, developed on the `experimental/xash-transcode` branch) is tracked in the sibling repo instead: [dod-web-demo-viewer#1](https://github.com/ccoventry/dod-web-demo-viewer/issues/1).

### Demo Analyzer Load Performance (see `docs/demo_analyzer_load_performance.md`)
- [x] Tier 1a — on-disk analyzer cache (cold parse ~1.3s, warm cache hit ~10-15ms).
- [x] Tier 1b — real progress events for the analyzer's cold-parse path.
- [x] Tier 2 — deleted dead `parse_with_diagnostics`/`ParseDiagnostics` code (~440 lines).
- [x] Tier 3 — Capture Studio folder scans now warm the analyzer cache via `scan_demo_for_highlights_with_analysis`.
- Tier 4/5 (selective netmessage parsing + `Delta` representation rewrite) is tracked as [issue #37](https://github.com/ccoventry/dod-tools/issues/37).

---

## ✅ Completed Tasks

### Connected Workspace — Phases 1-2 (2026-08-18, branch `feature/capture-block-manifest`, unpushed)
Plan: `~/.claude/plans/splendid-shimmying-kahn.md`. **Phases 3-4 still open; Phase 3 blocked on the manual verification above.**
- [x] **Phase 1 — capture block manifest + take verification.** `PatchJob` gained `Vec<CaptureBlock>` recording each recording block's resolved take folder, a `take_key` (`session_id/chain_JJ_bN`, via the new `shared::paths::take_key`), and `source_streak_indices` mapping back to the dispatched highlights — the overlap merge previously destroyed that mapping. After a batch (and after a cancel, since a cancelled batch still leaves real takes) each block is checked against disk and reported via a new `capture_takes_verified` event, two-tier: `captured` (folder exists, non-empty) and `renderable` (reuses Render Studio's own admission predicate, extracted as `hlcr::scanner::is_renderable_take` so the two can't silently disagree). Also removed two dead items: `CaptureJob.expected_take_folder` (never read, pointed at a path nothing writes) and `EngineEvent::Verified` (never constructed).
- [x] **Phase 2 — auto-Captured status.** Verified blocks advance every highlight they cover to `Captured`; status only ever moves forward, so a re-capture never knocks a `Rendered` highlight back. New `take_index.js` derives a content-addressed highlight uid (keyed on kill frames, not tick bounds, so a Kill Range edit doesn't orphan it) and preserves status/selection/notes/kill-range across a re-scan — which previously replaced demo objects wholesale and destroyed all of it.
- [x] **Bug: merged blocks silently dropped every highlight after the first.** The merge extended `end_tick`, but the record-stop mark derives from `kills[end_index]` — so recording stopped at the *first* highlight's last kill and anything merged in after it never reached the take, while the batch still reported success. Latent (highlights are life-bounded, so real ones rarely merge) but load-bearing for auto-status.
- [x] **Bug: Pre-Roll had no effect on capture timing.** The drop back to normal speed was hardcoded at 3.0s before record start; `pre_roll_seconds` only fed the merge decision. It now drives that mark. **Behaviour change:** the settle window is now whatever Pre-Roll says, not a fixed 3.0s — a saved 2.0 means 2.0s. Set Pre-Roll to 3.0 to keep the old timing.
- [x] **Bug: disk estimate billed the wrong window.** The pre-flight estimate used pre/post-roll while the AOT allocation uses start-lead/stop-trail (`calculate_total_capture_duration`) — a 2.6s/clip disagreement at defaults. The estimator now uses each window for what it governs: pre/post-roll to decide whether highlights collapse into one take, start-lead/stop-trail to bill frames. Start-lead/stop-trail also now refresh the launch guard, which they didn't despite feeding it.
- [x] **Two-tier block cutting (Scenario 1).** Highlights merge into one take only when their *recordings* overlap or sit within `MIN_TAKE_SEPARATION_SECONDS`; when only the roll windows collide they stay separate takes and the fast-forward between them is dropped instead (playback stays at normal speed across the gap, and the `stopsound` flush goes with it since there's no fast-forward drift to repair). Previously every roll-window collision collapsed into one clip full of dead air. Both tiers key off `first_kill_frame`/`last_kill_frame` so the decision uses the same frames the record marks do.
- [x] **`find_overlaps` diagnostic** (`cargo run -p native --bin find_overlaps -- <folder>`): scans a folder with the real highlight scanner and reports which highlights would merge, which would chain, and the tightest gap found with what roll values would trigger each tier. Built because merges are near-impossible to find by hand — highlights are life-bounded, so the closest pair across the whole `demos/` folder is ~19.5s apart, well outside any sane roll.

- [x] **Feature: Per-Tab Pinned Footer (2026-08-17):** The bottom status bar (`#pinned-action-bar`) was one global element identical on every tab — just the Capture Output free-space readout and a "Proceed to Capture" button, both Capture-Studio-specific, shown even on Render Studio/Demo Auditor/Demo Analyzer where they made no sense. Split into four `.pinned-footer-content` children in `index.html`, toggled by `switchNavTab()` (`nav.js`) the same way the tab panes themselves already are. Content per tab: Capture Studio keeps the Capture Output free-space indicator (dropped "Proceed to Capture" — it only ever jumped to the Configuration subtab, which the subtab buttons already reach, and Start Capture Batch already lives in-panel); Render Studio gets an Export Pool free-space readout + a live "N queued · N rendering · N done" summary (`render_pane.js`); Demo Auditor promotes its existing "Duplicates Found / Wasted Space" line into the pinned footer too (`auditor_pane.js`); Demo Analyzer shows the currently-loaded demo's filename (`analyzer_pane.js`). All four reuse data each pane already computes — no new backend calls.
- [x] **UI Cleanup: Removed Primary Media Dir, Renamed "Drive Overrides" → "Capture Output" (2026-08-17):** Now that Capture Output (formerly Drive Overrides) has real add/edit/remove UI (see the list-editor unification below), Primary Media Dir's only job — a single-path fallback for when that list was empty — was redundant and the "override" framing was backwards (it's not overriding anything, it's *where HLAE takes get written*). Deleted the field entirely: `index.html`'s input row, `capture_pane.js`'s `refreshLaunchGuard`/`buildCapturePayload` fallback branches (now hard-require at least one Capture Output entry, no silent single-path fallback), `main.js`'s persistence/hydration/browse handler, `CapturePayload.primary_media_dir` (`capture_manager.rs`), and `AppSettings.primary_media_dir` (`settings_manager.rs`). `config_from_payload` now derives `PatcherConfig.primary_media_dir` (the internal engine field — kept, still required by `native/src/patch/builder.rs`) straight from `payload.drives.first()` instead of an explicit-field-with-fallback chain. Renamed the tab label and all user-visible strings/comments from "Drive Overrides"/"Target Output Drive" to "Capture Output" (internal DOM ids like `#target-drive-list`/`tab-drive-overrides` were left alone — not user-visible, not worth the extra diff).
- [x] **Feature: Unified Editable-List Widget Across Drive/Command Lists (2026-08-17):** Drive Overrides and Render Folders (`#target-drive-list`/`#render-folder-list`) had no remove/edit/reorder at all — add-only, plain text `<li>`s. Render Studio's Export Drives had delete but no edit/reorder; Init/Custom Commands had inline edit + delete but no reorder, via a separate hand-rolled implementation. Extracted a shared `list_editor.js` widget (add, inline edit, remove, ⬆/⬇ reorder, optional native-picker browse via `@tauri-apps/plugin-dialog`, optional uniqueness) driving all five lists off one implementation; `index.html`'s three `<ul>` containers became `<div class="list-editor-list">` to match. Also replaced the 🗑 emoji remove icon with an inline SVG (`stroke="currentColor"`) after noticing WebView2 renders the emoji as a monochrome glyph that ignores CSS `color` — it was reading as a pause icon in `--text-muted` grey instead of respecting the grey→red hover state.
- [x] **Feature: Tauri & Vite Integration:** Migrated frontend stack from native `egui` to Tauri + Vite, establishing async IPC boundaries for demo ingestion, settings persistence, demo auditor, and AOT simulation.
- [x] **Headless Preview CLI Generator:** Built secondary `preview_cli` binary target ([main.rs](file:///c:/Users/Chris%20Coventry/Repos/dod-tools/native/src/bin/cli/main.rs)). Supports drag-and-drop folder/file execution, localized terminal output, and interactive/headless modes.
- [x] **Top Navigation Bar Migration & Non-Destructive Scan Cancellation:** Extracted Export Manager view, migrated sidebar to Top Navigation Bar, implemented non-destructive scan cancellation (`INGESTION_CANCEL`), and scaffolded localized footers across views.
- [x] **Localization System & Complete String Audit:** Localized all GUI, scanner weapon names, and CLI string literals. Updated `analysis::localization` to resolve both hashed (`#key`) and un-hashed (`key`) localization entries seamlessly.
- [x] Task: Magic Number Audit — Scanned codebase for hardcoded magic numbers and extracted shared constants.
- [x] Refactor: Isolate native engine stream slicing mechanisms behind explicit `target_arch` macro controls.
- [x] **HLTV Active Frame Injection:** Implemented and verified active standalone `DRC_CMD_INEYE` frame injection in `native/src/patch/engine.rs` with dynamic `target_player_id` extraction.
- [x] **Dynamic Drive Failover:** Implemented AOT capture routing, duration math parity, JIT render routing, and UI/UX export pool indicators with dynamic vector list reordering.
- [x] **Long Demo Validation & Packet Audit:** Verified parsing and slicing stability on extended demo files (>30 mins).
- [x] **Global Deprecation & Cruft Purge:** Deleted legacy fallback paths, unused variables, and historical code workarounds across UI and patcher crates.
- [x] **Bug Fix: `capture_directories` Drive Junction Abort:** `capture_pane.js` now builds `capture_directories` from the Target Output Drives pool (`state.targetDrives`), falling back to Primary/Backup Media Dir when that pool is empty, and toasts an error instead of dispatching when no output directory is configured at all — no longer sourced from `scanPaths`.
- [x] **Bug Fix: Un-stamped `session_id` Timestamp:** `generateSessionId()` in `capture_pane.js` stamps `session_YYYYMMDD_HHMMSS` into every outbound payload; `config_from_payload()` in `capture_manager.rs` extracts `payload.session_id` into `PatcherConfig.session_id`.
- [x] **Feature: Global & Per-Demo Bookmark Previews (`viewdemo`):** Reverted the per-streak preview ▶ buttons. Added "Launch Preview" (per-demo) and "Generate All Previews" (global) to `detail_pane.js`/`index.html`. Backend commands `launch_demo_preview`/`generate_all_previews` (replacing `launch_live_preview`) patch bookmarked `<stem>_preview.dem` files via `build_preview_patch_jobs`' existing per-highlight `svc_director` STUFFTEXT event injection, and the single-demo path launches HLAE directly with `+viewdemo <stem>_preview` (the old `primer_preview.dem` relay is gone).
- [x] **Bug Fix / UI Polish: Detail Pane Selection Checkbox & String Formatting (verified 2026-08-13, no code change needed):** Audited `detail_pane.js` against a request to restore the "Sel" checkbox and fix a leading-comma/missing-weapon formatting bug — both were already fixed in `82bf4a0` ("restore backend-supported highlight-table controls lost in migration"). The "Sel" column already renders `<input type="checkbox" class="streak-select-cb">` bound to `streak.selected`; `updateStreakVisuals()`'s `.map().join(', ')` never prepends a leading comma and always includes the weapon name, matching `CaptureStreak::update_visuals` in `native/src/patch/types.rs`.
- [x] **Bug Fix: Detail Pane — 3rd re-verification, defensive hardening added (2026-08-13):** Same claim re-filed with a specific repro string (`". (+0:29) , (+0:06)"`). Traced every candidate source (join loop, Rust mirror, `analyzer_pane.js`'s unrelated weapon grouping, the full `weapon.*` localization table) and found no path that produces it. Added `data-index="${streakIdx}"` to the checkbox per spec, and hardened `weaponClean` in `updateStreakVisuals()` with `.trim() || 'Unknown'` so an unresolved/empty weapon name can never leave a blank `Array.join()` element — the one theoretical (but unreproduced) root cause for an orphaned separator.
- [x] **Bug Fix: Detail Pane — actual root cause found from screenshot, two real bugs fixed (2026-08-13):** The prior two passes checked JS join logic and localization *data* (both correct) but not whether the app could *find* the localization file at runtime. (1) `styles.css`'s global `appearance: none` reset stripped native checkbox/radio rendering app-wide with no replacement — every checkbox including "Sel" was invisible but still clickable; exempted `input[type="checkbox"], input[type="radio"]` from the reset with themed `accent-color`. (2) `analysis::localization::load_pass()` (`analysis/src/localization.rs`) only checked CWD-relative paths and one `.parent()` hop from the exe, which never resolves to the workspace-root `localizations/` folder from the Tauri app's actual runtime working directory (`desktop-studio/src-tauri/target/...`) — every `translate_key("weapon.*")` call silently returned `None` and cached that failure process-wide, so every weapon name rendered as `""`, producing "N kills" fallbacks for single-kill streaks and orphaned `(+m:ss)` fragments (no weapon name) for multi-kill streaks. Fixed by walking up to 6 parent directories from the exe path looking for `localizations/` instead of assuming a fixed depth.
- [x] **UI Polish: Tab Branding & Styling (2026-08-13):** Renamed the nav tab "Workspace & Master Queue" → "Capture Studio" in `index.html`. Added a themed SVG chevron background to the global `select {}` rule in `styles.css` (the pre-existing `appearance: none` reset had removed the native arrow with no replacement, so every dropdown looked like a plain text field); stripped the two inline `style="..."` selects (`#render-codec-select` in `index.html`, `.streak-status-select` in `detail_pane.js`) that would otherwise override the chevron via inline-style specificity, preserving `.streak-status-select`'s dynamic per-status `color`.
- [x] **UI Cleanup: Dead POV Input Field (2026-08-13):** Deleted `#config-hide-non-pov`, its `<label>`, and the wrapper `.control-group` from `index.html`; deleted the corresponding unused `hideNonPovCheckbox` query from `detail_pane.js`. The POV filter (`isHLTV`/`recPlayer` check in `renderDetailView`) never read this checkbox — it was already unconditional — so no downstream logic changed.
- [x] **Custom Engine Commands Integration (2026-08-13):** Added a 4th "Custom Commands" tab to the Export Configuration panel (`index.html`) with an Init Commands list (text input + Remove) and a Custom Commands list (command text, Before/After `<select>`, offset-seconds number input, Remove) per row. `capture_pane.js` holds `initCommands`/`customCommands` as local module state (nothing else in the app needs them), with render functions wiring Add/Remove/edit handlers; extracted the existing inline payload-assembly code into a named `buildCapturePayload(state)` function (as referenced by this and the capture-directories task) and appended `init_commands`/`custom_commands` to its output, dropping blank rows. Backend: added `CustomCommandPayload { command, relation: String, offset_seconds }` DTO to `capture_manager.rs` and two new `#[serde(default)]` `CapturePayload` fields; `config_from_payload()` maps `init_commands` straight through and converts each `CustomCommandPayload` into `native::patch::CustomCommand`, parsing `relation` with a safe `_ => Before` fallback for any unrecognised value (mirrors the existing `allocation_strategy` string-match pattern) rather than rejecting the batch. Both fields were already fully consumed downstream by `build_batch_queue`/`builder.rs` (`config.custom_commands`/`config.init_commands`) — this closes the last gap between that existing engine support and the UI.
- [x] **`PatcherConfig` Full Persistence (2026-08-13):** Expanded `AppSettings` (`settings_manager.rs`) with `resolution_width`/`resolution_height`, `separate_hud`, `add_condebug`, `auto_clear_logs`/`auto_clear_previews`/`auto_clear_temp_demos`, `record_start_lead`/`record_stop_trail`, `initial_delay`, `fast_forward_speed`, `primary_media_dir`, `backup_media_dir`, `target_drives`, `init_commands`, and `custom_commands` (reusing `capture_manager::CustomCommandPayload`) — every new field is `#[serde(default = ...)]` so existing `settings.json` files on disk keep loading. `main.js`'s `persistAppSettings()`/startup hydration now scrape and restore every Export Configuration DOM field, rebuild the Target Output Drive `<li>` list, and round-trip the Custom Commands tab via two new `capture_pane.js` exports, `getCommandsState()`/`hydrateCommandsState()`.
- [x] **Pre-Flight Disk Allocation & Pre-Scan Estimator (2026-08-13):** Added `computeRequiredCaptureBytes()` (`capture_pane.js`) — merges each selected streak's pre/post-roll window with any others that overlap *within the same source demo* before billing bytes, so overlapping highlights are never double-counted; base cost is `w * h * 3` bytes/frame at the configured capture FPS, tripled when `separate_hud` is checked. Replaced the old `simulateAotCapacity`-based soft check (which only warned post-click, and silently bypassed the warning whenever `availableBytes === 0`) with an exported `refreshLaunchGuard()` that hard-disables `#start-capture-btn` and shows a new `#disk-space-warning-banner` whenever required bytes exceed the effective drive pool (Target Output Drives, falling back to Primary/Backup Media Dir) — including the zero-drive case. Wired to recompute live on resolution/HUD/timing field changes, target-drive add/browse, and streak-selection toggles (new `onSelectionChange` callback threaded through `detail_pane.js`'s `initDetailPane`).
- [x] **Running Process Guard & Detector Modal (2026-08-13):** Added `check_engine_processes`/`kill_engine_processes` Tauri commands (`capture_manager.rs`, backed by the new `sysinfo` dependency) that detect and force-kill any running `hl.exe`/`hlae.exe`. Added a "Half-Life Preview Detector" modal (`#process-detector-modal` in `index.html`, reusing the previously-unused `.modal`/`.modal-content` CSS) with Force Relaunch / Copy View Command / Cancel actions. `detail_pane.js`'s Launch Preview handler now calls `check_engine_processes` first and parks the launch args (`pendingLaunch`) instead of proceeding if a conflict is found; Force Relaunch kills the stragglers, waits 500ms, then replays the parked launch; Copy View Command builds `viewdemo <stem>_preview` from the active demo's filename and copies it via `navigator.clipboard.writeText`.
- [x] **Bug Fix: Save/Load Project Session Failing on Every Path (2026-08-16):** `@tauri-apps/plugin-fs`'s `readTextFile`/`writeTextFile` only succeed for paths under the app's own AppConfig/AppData dirs (`fs:default`'s real scope) — they can't touch a path a save/open dialog returns, so Save/Load Session errored everywhere. Added unscoped `save_project_session`/`load_project_session` Tauri commands (`std::fs`, same pattern as `save_settings`) and switched `main.js` to call those instead of the fs plugin directly.
- [x] **Feature: Canvas Timeline Relocated to Advanced Diagnostics (2026-08-16):** Moved the killstreak timeline `<canvas>` out of the always-visible Highlight Details header into the collapsed-by-default Advanced Diagnostics `<details>` block alongside Match Telemetry. Added a `toggle` listener in `detail_pane.js` that redraws on expand, since a canvas inside a collapsed `<details>` has zero `clientWidth`/`clientHeight` and would otherwise stay blank the first time it's opened.
- [x] **Bug Fix: Master Demo Queue Highlight Counts & Status Column (2026-08-16):** The queue's "Yield" column (renamed "Highlights") summed *every player's* streaks per demo instead of just the recording player's — a demo could read "192/192" purely because streaks default to selected until deselected, not because anything was actually captured. Root cause traced further: the POV filter gated on `demo.is_pov`, which reflects any `SvcHltv`/`SvcDirector` message anywhere in the file — a signal that also fires on an ordinary player-recorded demo whenever an HLTV caster merely spectated the live match (server-broadcast messages every connected client receives), not a reliable "no single owner" flag. `scan_demo_for_highlights` already rejects true HLTV proxy files before `local_player_index` is ever computed, so fixed both `detail_pane.js`'s row filter and a new `master_pane.js` `recordingPlayerStreaks()` helper to gate on whether `local_player_index` resolved instead. Also redesigned the queue's columns per user feedback: Highlights is now a plain total (not a selected/total fraction), the misleading per-row Status badge (just whichever streak's status was touched most recently) is gone, and a bare Actions column (remove-from-queue only) moved to the end. Status edits and streak-selection toggles in the Highlight Details table now both re-render the queue live via `renderMasterList`, which previously went stale on either.
- [x] **Bug Fix: Opt-In `streak.selected` Semantics (2026-08-16):** `streak.selected` sits at `undefined` until a row is actually rendered in the Highlight Details table (opt-in default, set to `false` on first render) — but `computeRequiredCaptureBytes` (disk estimator), `buildCapturePayload` (the actual capture-batch payload builder), and `renderTimeline`'s selection-color logic all treated `undefined` as *selected* (opt-out semantics), the opposite of the real default. Beyond inflating the disk estimate and mis-coloring the timeline, `buildCapturePayload`'s version meant clicking Start Capture Batch on a demo never opened in the detail view would have captured every highlight from every player in it. All three now require `selected === true`.
- [x] **Bug Fix: Capture Studio Configuration Auto-Save Gaps (2026-08-16):** Audited every field feeding `persistAppSettings()`/`AppSettings` for a working save trigger. Missing entirely: Timing Options' Start Lead/Stop Trail/Initial Delay, the whole Custom Commands tab (Init/Custom Commands add/edit/remove), Add Condebug, the three Auto-clear checkboxes, and Primary Media Dir's Browse button (its handler set `.value` directly, which doesn't fire an `input` event, so the field's existing listener never ran). Routed all of them through a new `onSettingsChange` callback threaded into `initCaptureUI` (`main.js` passes `persistAppSettings`), same pattern `refreshLaunchGuard` already used for the guard-feeding fields.
- [x] **Feature: Persist Save Local Patched Copy, Allocation Strategy & Render Studio Settings (2026-08-16):** These were read live from the DOM whenever needed but never in `AppSettings`, so they silently reset every restart. Added `save_local_patched_copy`, `allocation_strategy`, `render_folders`, `render_codec`, `render_fps`, `render_max_concurrent`, `render_export_dirs` to `AppSettings` (all `#[serde(default)]`), wired save-on-change for all of them (`initRenderUI` gained the same `onSettingsChange` callback as `initCaptureUI`), and added the `<label>` Allocation Strategy's `<select>` was missing (unlike every other Configuration control, which made it easy to miss as an actual setting).
- [x] **Bug Fix (critical): Batch Capture Never Actually Patched Demos (2026-08-17):** `build_batch_queue()` (`native/src/patch/builder.rs`) only ever *planned* each patched demo — output path, drive routing, scheduled commands — it never called `StreamPatcher::patch()` to write the bytes. `capture_manager.rs` handed that job list straight to the capture engine, which assumes `patched_demo_path` already exists and just copies it into `dod/`, so every batch failed immediately with "Failed to open source demo for copy: file not found." Only the separate Preview pathway ever called `StreamPatcher::patch()`; the batch path never got the equivalent step during the Tauri port (a stale comment in `builder.rs` references "the inline patch_worker in `capture/select.rs`," a module that doesn't exist in this codebase — evidently a step the old pre-Tauri app had that was dropped along the way). Added the missing patch loop in `capture_manager.rs` right after `build_batch_queue()` returns, with cancel-token checks and `capture_status` progress events between jobs (same event shape the frontend's progress bar already renders). Verified end-to-end through Capture Studio and Render Studio.
- [x] **Feature: Session File Memory & UX (2026-08-17):** Save Session always opened a fresh Save-As dialog even right after loading or saving a specific file. Added `currentSessionPath` tracking (`main.js`) so subsequent saves write straight back to the same file, falling back to Save-As only the first time in a window — matches Ctrl+S in every other app. Added a small filename indicator beside Save/Load Session (full path on hover). Fixed Ctrl+O to always open Load Session regardless of which Capture Studio sub-tab is active (previously opened Add Demo Files when on Highlights, inconsistent with Ctrl+S). Fixed Save Session silently no-op'ing (`console.warn` only) when nothing's been scanned yet — now shows a toast explaining why.