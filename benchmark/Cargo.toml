[package]
name = "dod-benchmark"
version.workspace = true
edition.workspace = true

[[bin]]
name = "dod-benchmark"
path = "src/main.rs"

[dependencies]
analysis = { path = "../analysis" }
dem = { workspace = true }
dod = { path = "../dod" }
serde_json = { workspace = true }
