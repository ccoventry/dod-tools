# Vendored fork of the `dem` crate (https://github.com/khanghugo/dem), wired in
# from the workspace root via [patch.crates-io] and listed as a workspace member
# so it is covered by `cargo test --workspace` and `cargo clippy --workspace`.
#
# This file used to be the crates.io-normalized manifest, header comment and all,
# carried over verbatim when the crate was vendored. That left `autotests = false`
# and `exclude = ["src/tests"]` set, which — combined with the crate not being a
# workspace member — meant the bit-level demo reader/writer had no test or lint
# coverage at all. Rewritten 2026-09-10.
#
# Upstream authorship and licence are unchanged; see LICENSE and the repository
# link below.

[package]
name = "dem"
version = "0.2.3"
edition = "2021"
authors = ["Khang Le <mkhangle20@gmail.com>"]
description = "GoldSrc demo parser and writer library"
readme = "README.md"
keywords = [
    "goldsrc",
    "goldsource",
    "demo",
]
license-file = "LICENSE"
repository = "https://github.com/khanghugo/dem"

[lib]
name = "dem"
path = "src/lib.rs"

[dependencies]
bitvec = "1.0.1"
eyre = "0.6.12"
# Deliberately NOT `{ workspace = true }`. The workspace is on nom 8, and this
# crate's ~2,900 lines of parser code are nom 7: nom 8 turns every combinator
# into an `impl Parser` that needs `.parse(i)` rather than a callable, which is
# 129 compile errors here. Migrating the bit-level demo reader/writer is real
# work with real risk and wants its own change, not a ride-along in a cleanup
# pass. Until then this compiles a second copy of nom, which is the cheaper of
# the two costs.
nom = "7.1.3"
